# Urule Roadmap

This document tracks improvements, fixes, and features across the entire Urule ecosystem. Items are organized by priority and category. Each item includes sub-tasks scoped to specific repos/packages.

**Want to contribute?** Pick any unchecked item, open an issue referencing it, and submit a PR. See [CONTRIBUTING.md](CONTRIBUTING.md) for guidelines.

---

## Human Todo — needs a browser / interactive session

Items here can't be resolved from CI logs or headless test runs — they need someone at a real browser with devtools (or `playwright --headed --debug`) to observe what's actually happening. AI-driven debugging got the symptom pinned down; the cause needs eyes on the live page.

- [ ] **office-ui — a hard navigation (`page.reload()` / `page.goto`) to an auth-gated route redirects to `/login` despite valid auth in localStorage** (blocks the last 3 specs in #65). Symptom (confirmed locally with instrumentation): after the auth fixture demo-logs-in and lands on `/office`, a test that does `page.reload()` (notification-center reload specs) or `page.goto('/setup')` (onboarding `should display provider options`) ends up on `/login` — and at that exact moment `localStorage['urule-auth']` still contains `{"state":{...,"isAuthenticated":true},...}`. So the auth-redirect `useEffect` in `apps/office-ui/src/app/office/layout.tsx:42-47` (and the equivalent in `apps/office-ui/src/app/setup/page.tsx`) is firing with `isAuthenticated === false` even though the persisted value is true, meaning #80's `authHydrated` gate isn't fully closing the zustand-persist hydration race after a *hard* navigation. Same-tab soft navigations (the fixture's own `router.push("/office")`, and all the non-reload notification specs) work fine — only the hard-navigation path breaks. **Notably it reproduces in CI's `next build` + standalone server but NOT under local `next dev`** (`next dev` apparently hydrates fast enough to dodge the race), so a local repro needs the production build, not the dev server — see below.
  - **Repro (production build, matches CI)**: `cd apps/office-ui && npx next build --no-lint && PORT=3000 HOSTNAME=0.0.0.0 node .next/standalone/server.js &` then in another shell `npx playwright test notification-center.spec.ts onboarding.spec.ts --grep "clear all|provider options" --project=chromium --headed --debug` — first change `test.fixme` → `test` on `notification-center.spec.ts` lines ~51 and ~90, and apply the onboarding rewrite from PR #87 (use `authenticatedPage`, goto `/setup`, assert the "Select AI Model" heading). If your backend services aren't running, set `NEXT_PUBLIC_REGISTRY_URL` etc. to dead ports so the API calls fail fast like CI — see the `SERVICE_URLS` map in `apps/office-ui/src/lib/api.ts`.
  - **What to look for**: step to the `page.reload()` / `page.goto('/setup')`. Open React DevTools → find `OfficeLayout` / `SetupPage` → watch `useAuthStore`'s `isAuthenticated` and the `authHydrated` flag (`useAuthHasHydrated`) as the component re-mounts. Is `isAuthenticated` false when the redirect-`useEffect` fires? Check the Network tab for the timing of the `/login?_rsc=...` request relative to the navigation. Candidate fixes once confirmed: have `useAuthHasHydrated` only return `true` once it has *observed* the post-hydration state (not just `persist.hasHydrated()`); or have the redirect effect read `useAuthStore.getState().isAuthenticated` fresh inside the effect; or fall back to a direct `localStorage.getItem('urule-auth')` parse before redirecting. The fix likely belongs in `apps/office-ui/src/hooks/useAuthHasHydrated.ts` and/or the two layout/page effects.
  - **When fixed**: re-enable `notification-center.spec.ts:43` (`a programmatic toast surfaces in the notification list`) + `:90` (`clear all removes every entry`), and apply the onboarding rewrite (PR #87 had it — `authenticatedPage` + goto `/setup` + assert "Select AI Model" heading, which replaces the broken `page.textContent('body').includes('Provider')` guard that trips on `/login`'s inlined `<script>` RSC blob). Confirm CI green, then delete this item. That closes #65.

- [ ] **mcp-gateway — uncommitted WIP of unknown provenance in the working tree** (discovered 2026-05-18 while shipping #4 Phase F on `urule-ai/mcp-gateway`). Two files in the `mcp-gateway` repo were modified but committed on no branch: `src/middleware/error-handler.ts` (adds a `hasZodFastifySchemaValidationErrors` branch returning the `{ error: 'Validation failed', details }` envelope) and `src/routes/tools.routes.ts` (adds Zod `querystring`/`params` schemas + OpenAPI `schema` blocks to both `GET /api/v1/mcp/tools` routes). The change looks coherent and intentional — wiring the tools routes into the type-provider Zod validation, matching how the other route files already work — but its origin is unknown.
  - **Investigate**: check developer machines / `git stash` / `git reflog` on the `mcp-gateway` repo for related work; figure out whether this was a deliberate change left mid-stream and whether a larger task around it is unfinished (e.g. were other route files meant to get the same treatment?).
  - **When resolved**: either commit it as its own small PR (`feat: wire mcp-gateway tools routes into Zod validation`) or discard it. It was deliberately excluded from mcp-gateway Phase F (PR #1) — not part of the #4 authz work — and floats uncommitted until someone decides. Then delete this item.

---

## 1. Security (Critical)

These items must be addressed before any production deployment.

> ⚠️ **§1.1–§1.6 below reflect an earlier internal hardening pass.** The **2026-05-08 external penetration test** (tracking issue [#20](https://github.com/urule-ai/urule/issues/20) — 12 critical / 14 high / 9 medium / 3 low) surfaced findings those subsections do **not** cover; remediation is tracked in **§1.7** and is largely **not yet complete**. Do not read "§1.1 ✅" as "security is done".

### 1.1 Authentication Middleware ✅
Add JWT validation middleware to all service routes.

- [x] **registry** — Add `@fastify/jwt` plugin, validate Bearer tokens on all `/api/v1/*` routes
- [x] **langgraph-adapter** — Add JWT middleware to chat, runs, and WebSocket endpoints
- [x] **approvals** — Add JWT middleware; extract user identity for approval audit trail
- [x] **mcp-gateway** — Add JWT middleware to server registration and binding routes
- [x] **channel-router** — Add JWT middleware (except webhook ingestion endpoints which use HMAC)
- [x] **packagehub** — Add JWT middleware to publish/version routes (read routes can be public)
- [x] **state** — Add JWT middleware to presence and task ownership routes
- [x] **governance** — Add JWT middleware; this service validates auth for others
- [x] **runtime-broker** — Add JWT middleware to session allocation routes
- [x] **backstage plugin** — Add service-to-service auth token validation
- [x] **Shared**: Create `@urule/auth-middleware` package with reusable Fastify plugin

### 1.2 Input Validation ✅
Add request body/query validation on all API routes.

- [x] **registry** — Validate agent creation, workspace updates, provider creation, conversations, auth with Zod schemas
- [x] **approvals** — Validate approval request body, approve/deny/escalate/request-changes
- [x] **mcp-gateway** — Validate MCP server registration, tool registration, binding creation
- [x] **channel-router** — Validate channel binding, identity mapping, send message payloads
- [x] **state** — Validate room creation (capacity, type), presence, tasks, widget state updates
- [x] **packagehub** — Validate package publish payload and search query parameters (limit, offset)
- [x] **langgraph-adapter** — Validate run start params, chat message, chat actions, artifacts
- [x] **runtime-broker** — Validate session allocation request
- [x] **All services**: Using Zod `safeParse()` with 400 error responses including detailed issue descriptions

### 1.3 CORS Lockdown ✅
Replace `origin: true` (allow all) with explicit origin whitelist. All 10 services that office-ui calls from the browser ([apps/office-ui/src/lib/api.ts](apps/office-ui/src/lib/api.ts) `SERVICE_URLS` map) now register `@fastify/cors` with `process.env.CORS_ORIGINS` (comma-split, defaults to `http://localhost:3000`). The backstage plugin is intentionally excluded — it's a backend-to-Backstage sync, not browser-facing.

- [x] **registry, packagehub, state, langgraph-adapter, approvals** — Original wave (configurable via `CORS_ORIGINS`).
- [x] **governance, packages, mcp-gateway, channel-router, runtime-broker** — Added in expansion wave: same canonical pattern, `@fastify/cors` dep added to each `package.json` (mcp-gateway already had it). Without this, browser preflight from a non-localhost origin would have produced no `Access-Control-Allow-Origin` header in production and the UI couldn't have called these services.

### 1.4 Rate Limiting ✅
Add `@fastify/rate-limit` to prevent abuse.

- [x] **All 11 services** — `@fastify/rate-limit` with 100 req/min per IP
- [x] **langgraph-adapter** — Stricter limit: 30 req/min (AI execution is expensive)

### 1.5 Environment & Config Validation ✅
Validate required environment variables at startup; fail fast if missing.

- [x] **9 services** — `validateConfig()` checks DATABASE_URL, NATS_URL, REGISTRY_URL at startup
- [x] **governance** — Warns if `OPENFGA_STORE_ID` is empty
- [x] **registry, packagehub, mcp-gateway** — Removed hardcoded `urule:urule@localhost` defaults; `validateConfig()` now throws (not warns) when `DATABASE_URL` or `NATS_URL` is missing
- [x] **langgraph-adapter** — Added `src/middleware/error-handler.ts` with `redactSecrets()` covering `sk-ant-…`, OpenAI `sk-…`, `Bearer …`, `authorization:`, `x-api-key:`, and `?api_key=`/`?access_token=` query params. Redacts both response bodies AND log lines (incl. `error.message` and `error.stack`)
- [x] **state, governance, approvals, channel-router, langgraph-adapter, runtime-broker** — Extended fail-fast pattern to remaining 6 services. `validateConfig()` in each now throws on missing `NATS_URL` (and `TEMPORAL_ADDRESS` for approvals, `REGISTRY_URL` for langgraph-adapter); governance keeps `OPENFGA_STORE_ID` warn-only since dev setups intentionally skip authz. Each service has a `tests/config.test.ts` covering missing/present cases (+14 tests total).
- [x] **registry, packagehub, governance, mcp-gateway** — Lifted `redactSecrets` into `@urule/events` (`src/redaction/redact-secrets.ts`) with 9 unit tests. All four services' `src/middleware/error-handler.ts` now redact `error.message` and `error.stack` before logging *and* before returning to the client. langgraph-adapter switched from its local copy to the shared util; its duplicate tests were removed (the canonical tests live in `@urule/events`).

### 1.6 Audit Logging ✅
Track who did what and when for compliance.

- [x] **registry** — Log agent creation/update/status, provider CRUD, auth login with user identity
- [x] **approvals** — Log approve/deny/escalate decisions with approver identity
- [x] **governance** — Log policy evaluations and authz check denials
- [x] **mcp-gateway** — Log MCP server registration and deletion
- [x] **Shared**: `AuditLogger` class + `AuditEvent` type + `AUDIT_TOPICS` in `@urule/events`

### 1.7 External pen-test remediation (2026-05-08)
The 2026-05-08 external security audit (tracking [#20](https://github.com/urule-ai/urule/issues/20)) filed its findings as GitHub issues #1–#19 (with #22–#26 cross-listed from the quality audit). §1.1–§1.6 above predate it. Remediation is sequenced: **Tier 0** (dependency CVEs + CI gating) → **Tier 1** (surgical critical/high fixes, one PR each) → **Tier 2** (architectural, each its own design + PR).

**Tier 0 — done:**
- [x] [#2](https://github.com/urule-ai/urule/issues/2) — `@fastify/jwt` ^9→^10 (pulls `fast-jwt` 6.2.4) — clears the critical `crit`-header advisory
- [x] [#3](https://github.com/urule-ai/urule/issues/3) — `next` 14.2.5→14.2.35 — clears CVE-2025-29927 (middleware auth bypass, CVSS 9.1) + the middleware-redirect SSRF/cache-poisoning advisories; residual App Router advisories → [#88](https://github.com/urule-ai/urule/issues/88)
- [x] [#22](https://github.com/urule-ai/urule/issues/22) — npm-audit CVE sweep: 23→17 findings, **2 critical → 0**; residuals (need major bumps) → [#88](https://github.com/urule-ai/urule/issues/88), [#89](https://github.com/urule-ai/urule/issues/89)
- [x] [#19](https://github.com/urule-ai/urule/issues/19) — CI no longer swallows `npm audit` / typecheck / build failures (gates on `--audit-level=critical`; tighten to `high` once #88/#89 land)
- [x] [#42](https://github.com/urule-ai/urule/issues/42) — CI uses `npm ci`, not `npm install`

**Tier 1 — done (surgical critical/high, one PR each):**
- [x] [#1](https://github.com/urule-ai/urule/issues/1), [#13](https://github.com/urule-ai/urule/issues/13) (PR [#92](https://github.com/urule-ai/urule/pull/92)) — auth-middleware fails closed by default on JWKS error; dropped the hardcoded `'account'` audience acceptance
- [x] [#5](https://github.com/urule-ai/urule/issues/5) (PR [#93](https://github.com/urule-ai/urule/pull/93)) — `GET /api/v1/providers/:id/key` is now `admin`-only; non-admin / anonymous → 403
- [x] [#12](https://github.com/urule-ai/urule/issues/12), [#46](https://github.com/urule-ai/urule/issues/46) (PR [#94](https://github.com/urule-ai/urule/pull/94)) — docker-compose credentials required via `${VAR:?}`; insecure dev defaults moved to `infra/compose/.env.example`
- [x] [#25](https://github.com/urule-ai/urule/issues/25) (PR [#96](https://github.com/urule-ai/urule/pull/96)) — `GET /api/v1/agents` (cross-workspace list) is `admin`-only; sibling routes + office-ui callers tracked in [#95](https://github.com/urule-ai/urule/issues/95)
- [x] [#24](https://github.com/urule-ai/urule/issues/24), [#45](https://github.com/urule-ai/urule/issues/45) (PR [#97](https://github.com/urule-ai/urule/pull/97)) — pinned the 7 `:latest` infra images (Temporal × 2, Keycloak, OpenFGA, OPA, OTel, Jaeger); Helm `imageTag` default → `"0.1.0"`
- [x] [#18](https://github.com/urule-ai/urule/issues/18), [#40](https://github.com/urule-ai/urule/issues/40) (PR [#98](https://github.com/urule-ai/urule/pull/98)) — audit logger routes through Fastify's Pino logger (redaction); `.catch(() => {})` swallows replaced with `request.log.warn`
- [x] [#16](https://github.com/urule-ai/urule/issues/16) (PR [#99](https://github.com/urule-ai/urule/pull/99)) — `canonicalDigest` recursively sorts object keys at every level (the old replacer-array form silently dropped nested keys, defeating signature binding)
- [x] [#23](https://github.com/urule-ai/urule/issues/23) (PR [#100](https://github.com/urule-ai/urule/pull/100)) — non-root `USER` in all 7 in-repo Dockerfiles; Helm `global.podSecurityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }` default. Standalones (separate repos) still need their own Dockerfile `USER` fix.

**Tier 2 — architectural (each its own design + PR):**
- [ ] [#4](https://github.com/urule-ai/urule/issues/4) — system-wide authorization (currently only authn is enforced; no resource-level authz)
- [ ] [#9](https://github.com/urule-ai/urule/issues/9) — packagehub: require auth + publisher-ownership on publish routes
- [ ] [#10](https://github.com/urule-ai/urule/issues/10) — sandbox the package installer (SSRF / arbitrary file read / arbitrary git clone)
- [ ] [#6](https://github.com/urule-ai/urule/issues/6) — validate provider `PATCH` `baseUrl` (no localhost / metadata IP / private ranges)
- [ ] [#7](https://github.com/urule-ai/urule/issues/7) — agent prompt-injection / system-prompt takeover
- [ ] [#8](https://github.com/urule-ai/urule/issues/8) — conversation message spoofing + LLM cost amplification
- [ ] [#11](https://github.com/urule-ai/urule/issues/11) — entitlement bypass (gate minting behind the verified Stripe webhook / admin-only)
- [ ] [#14](https://github.com/urule-ai/urule/issues/14) — office-ui tokens in localStorage → httpOnly cookies + CSRF token
- [ ] [#15](https://github.com/urule-ai/urule/issues/15) — `WidgetFrame` `postMessage('*')` + sandbox conflict + missing origin check
- [ ] [#17](https://github.com/urule-ai/urule/issues/17) — Stripe webhook trusts publisher-controlled `metadata.kind` (derive it server-side)
- [ ] [#26](https://github.com/urule-ai/urule/issues/26) — `@urule/llm-providers`: add retry / timeout / abort signal / usage tracking

**Dependency follow-ups:** [#88](https://github.com/urule-ai/urule/issues/88) (office-ui → Next 15+ — ~13 residual App Router GHSA advisories), [#89](https://github.com/urule-ai/urule/issues/89) (`@opentelemetry/*` sdk-node 0.55→0.217 + `vitest` 2→4, dev-tree).

---

## 2. Testing (High)

### 2.1 Unit Test Coverage ✅
Fill gaps in services that lack route-level tests.

- [x] **registry** — 6 route tests (auth validation, health check, mock user)
- [x] **packagehub** — 6 route tests (publish validation, version validation)
- [x] **langgraph-adapter** — 8 route tests (chat/run validation, capabilities)
- [x] **backstage plugin** — 3 route tests (catalog, scaffolder, health)
- [x] **channel-router** — Validation tests added to existing suite
- [x] **packages** — 5 route tests (install/upgrade validation)
- [x] **All services** — Zod validation tests cover invalid input (400s) across all services

### 2.2 E2E Integration Tests
Extend the Phase 1 E2E suite to cover all phases.

- [x] **Phase 2 E2E** — [infra/e2e/phase2.test.mjs](infra/e2e/phase2.test.mjs). Publishes a package to PackageHub (with rejected duplicate), publishes a version, browses for it, fetches by name and by name+version, then installs via the packages service into a workspace, lists installations, rejects duplicate install, fetches by id, uninstalls, verifies 404 on subsequent fetch.
- [x] **Phase 3 E2E** — [infra/e2e/phase3.test.mjs](infra/e2e/phase3.test.mjs). Exercises every approval transition: approve, deny, reject (terminal), cancel (requester withdraws), and escalate (priority bump). Creates an approval rule first to verify rule routing. Final list endpoint verifies all 5 approvals with correct statuses.
- [x] **Phase 4 E2E** — [infra/e2e/phase4.test.mjs](infra/e2e/phase4.test.mjs). State service: room create + task create + agent assignment + status update + cleanup. Channel-router: binding create + slack-shaped inbound webhook normalization + identity-mapping create/lookup + cleanup. Tolerant of impl variation (PATCH vs POST for task status, list vs object response shapes) so it doesn't break on response-shape evolution.
- [x] **Phase 5 E2E** — Widget lifecycle covered by [office-ui/e2e/widgets.spec.ts](apps/office-ui/e2e/widgets.spec.ts) under the §2.3 Playwright suite (built-in widget mounting + bridge contract + error-boundary). Browser-side, not a backend HTTP flow.
- [ ] **Phase 6 E2E** — Chat-with-AI flow needs an Anthropic API key + outbound network access; not appropriate for a CI E2E. Phase 1's run lifecycle + the langgraph-adapter unit tests cover the orchestrator boundary; the configure-API-key + install-personality parts are already exercised by Phase 2.
- [ ] **Phase 7 E2E** — Marketplace install + widget install + conversation branching + agent memory CRUD + MCP server registration journeys. The Playwright suite at [office-ui/e2e/](apps/office-ui/e2e/) has 18 specs covering most page-level flows but the test-coverage audit (2026-05) surfaced 5 user journeys that lack end-to-end specs against a running stack. Each is one new spec file (~30-50 lines) talking to phase6.yaml services. Adds confidence that the full happy path works after a fresh `make dev` boot — today only individual service tests exist for these flows.

Shared helpers live in [infra/e2e/lib.mjs](infra/e2e/lib.mjs) (assert / test / post / get / del / patch / waitForService / summary). [infra/e2e/run-all.mjs](infra/e2e/run-all.mjs) chains phases 1-4; honors `PHASE=N` to scope to a single phase. The Dockerfile copies all four phase files + helpers and defaults to running them all on container start.

### 2.3 UI Testing ✅
Browser-based testing for the Office UI. Playwright suite at [apps/office-ui/e2e/](apps/office-ui/e2e/) — 16 spec files, ~700 lines, organised by user journey. Authenticated tests use the [fixtures/auth.ts](apps/office-ui/e2e/fixtures/auth.ts) helper.

- [x] **office-ui** — Playwright set up. `@playwright/test ^1.48.0` in devDeps; `npm run e2e`, `e2e:ui`, `e2e:headed`, `e2e:report` scripts in package.json.
- [x] **office-ui** — Auth flow ([e2e/auth.spec.ts](apps/office-ui/e2e/auth.spec.ts)). Login page renders, validation errors fire on empty submission, invalid credentials produce server error (502 if Keycloak down), demo-mode flow.
- [x] **office-ui** — Agent management ([e2e/agents.spec.ts](apps/office-ui/e2e/agents.spec.ts)). Browse list, search, category filters; agent creation wizard surface verified.
- [x] **office-ui** — Chat interface ([e2e/chat.spec.ts](apps/office-ui/e2e/chat.spec.ts)). Conversation list filter tabs, new-chat button, conversation routing.
- [x] **office-ui** — Approval queue ([e2e/approvals.spec.ts](apps/office-ui/e2e/approvals.spec.ts)). Status filter tabs (Pending/Approved/Rejected), skeleton-loading transition.
- [x] **office-ui** — Responsive layout ([e2e/responsive.spec.ts](apps/office-ui/e2e/responsive.spec.ts)). Mobile hamburger menu, tablet/desktop sidebar visibility, viewport-driven layout shifts.
- [x] **office-ui** — Widget lifecycle ([e2e/widgets.spec.ts](apps/office-ui/e2e/widgets.spec.ts)). Built-in widget mounting (approval-queue, dashboard-stats, chat-list), widget bridge contract smoke (host shell persists across navigation), error-boundary smoke (single failing widget doesn't crash the page). Closes Phase 5 from §2.2.

Adjacent specs covering the rest of the office-ui surface, also part of this suite: dashboard, onboarding, projects, workspaces, settings, theme, integrations, logs, security, accessibility — for ~16 spec files total.

### 2.4 Security Testing
- [x] **All services** — Each of the 11 services now has `tests/auth-401.test.ts` (registry's lives in `tests/unit/`) that registers `@urule/auth-middleware` with `failClosed: true` + an unreachable `jwksUrl` and the same `publicRoutes` list as production, then asserts: (a) unauthenticated requests to a representative protected route return 401, (b) `/healthz` and `/docs/*` remain 200, (c) any service-specific public prefix (`/api/v1/infrastructure` and `/auth/login` for registry, `/api/v1/packages` for packagehub, `/api/v1/channels` for channel-router) also remains 200. The publicRoutes list is duplicated from server.ts deliberately so config drift in either direction shows up as a test failure.
- [x] **All services** — Tests verifying invalid input returns 400 (not 500) — landed in earlier waves (registry/packagehub/mcp-gateway/etc. `routes.test.ts` files exercise Zod 400s)
- [x] **registry, packagehub, mcp-gateway** — CORS validation tests added (`tests/{unit/,}security.test.ts` — preflight from non-allow-listed origin produces no `Access-Control-Allow-Origin`)
- [x] **All services** — Replaced today's `buildCorsApp()` test helpers (registry/packagehub/mcp-gateway) with tests that import `buildServer()` and exercise the real `process.env.CORS_ORIGINS` wiring; added equivalent `tests/cors.test.ts` to the seven services that lacked any CORS test (state, langgraph-adapter, approvals, governance, packages, channel-router, runtime-broker). Each suite asserts: first allow-listed origin echoed, second comma-separated origin echoed (proves the comma split), non-allow-listed origin rejected, and the unset-env fallback to `http://localhost:3000`. A typo like `CORS_ORIGIN` (singular) in the service code would now fail tests.
- [x] **Infra** — `npm audit --audit-level=high` step in `.github/workflows/ci.yml` (already landed; warn-only via `continue-on-error: true`)
- [x] **Cross-service** — Test coverage expansion (2026-05). Survey + targeted additions across the 6 highest-gap workspaces: registry HTTP integration tests (conversation CRUD + agent CRUD + memory CRUD, +20), governance HTTP route tests (decide / policy/evaluate / authz/check / authz/batch-check + audit-event capture, +12), packages entitlement-gate flow (free / paid no-entitlement → 402 / paid with-entitlement / packagehub-unreachable graceful-degrade / uninstall + concurrent install, +8), state multi-user collaboration journey (rooms + presence + tasks + typing across users + lifecycle transitions, +12), channel-router telegram + webhook adapter normalization (+14), and **first frontend Vitest setup in office-ui** with unit tests for `useInstalledWidgetsStore` + `extractWidgetManifest` shape validation (+18). Workspace-wide: 652 → 736 `it()` blocks, 115 → 124 test files, all green. Patterns established for future fan-out: HTTP integration via real `buildServer()` or inline Fastify with type-provider zod compilers + zod-aware error handler; mock-DB chain pattern for Drizzle services (no Postgres needed); cross-service `fetch` stubbed via `vi.stubGlobal`; fire-and-forget audit events captured via `console.log` spy + `setImmediate` flush; office-ui Vitest config separates unit tests (`src/**/*.test.ts`) from Playwright e2e (`e2e/**/*.spec.ts`).

#### Testing follow-ups (open)
- [ ] **office-ui** — Vitest fan-out to remaining stores / hooks / lib helpers. Today 18 unit tests cover `useInstalledWidgetsStore` + `extractWidgetManifest`. The other 12 zustand stores (`useAuthStore`, `useChatStore`, `useDashboardLayoutStore`, `useNotificationCenterStore`, `useThemeStore`, etc.), 8+ hooks (`useFormAutoSave`, `useFormHistory`, `useNotificationSounds`, `useApprovalEvents`, `useOnlineStatus`, `useSsoLogin`, `useWidgetConfig`, `useNotificationCapture`), and several lib helpers (`exportData`, `focusTrap`, `api`'s axios interceptors) are uncovered. Pattern is established; mechanical fan-out to lift coverage to ~60-80 tests. Highest-value targets: stores that touch persistence (cross-session bugs) and the axios interceptor's 401-refresh flow (security-critical).
- [ ] **mcp-gateway** — Tool invocation dispatch tests once §6.3 ships the `/invoke` route. The testing audit flagged this as a missing capability; the test gap is downstream of the missing feature.
- [ ] **approvals** — Audit event emission tests (depends on §6.4 NATS publishing landing). Concurrent-decision race condition coverage (two users approving the same approval simultaneously — should serialize cleanly) — surfaced by the audit.

---

## 3. UX & UI (High)

### 3.1 Error Handling ✅
Replace silent failures and `alert()` calls with proper UI feedback.

- [x] **office-ui** — Toast notification system (success/error/warning/info, auto-dismiss, stacking)
- [x] **office-ui** — React Error Boundaries with dark-themed fallback UI and retry
- [x] **office-ui** — Error feedback on failed API calls (replaced `.catch(() => {})` patterns)
- [x] **office-ui** — Replaced all `alert()` calls with toast notifications
- [x] **office-ui** — Network offline detection banner. New [hooks/useOnlineStatus.ts](apps/office-ui/src/hooks/useOnlineStatus.ts) (`navigator.onLine` + `online`/`offline` event subscription + 5s polling fallback for browsers that miss events; visibility-aware so it stops polling when the tab is hidden) and [components/layout/OfflineBanner.tsx](apps/office-ui/src/components/layout/OfflineBanner.tsx) (sticky top banner with `role="status" aria-live="polite"`; slides in/out via CSS transform). Mounted globally in [office/layout.tsx](apps/office-ui/src/app/office/layout.tsx). Test-id `offline-banner` for Playwright.
- [x] **office-ui** — Retry buttons on failed data fetches. New reusable [`QueryError`](apps/office-ui/src/components/ui/QueryError.tsx) component (`error`, `onRetry`, `label`, `compact`) is the drop-in pattern: `if (isError) return <QueryError error={error} onRetry={refetch} />`. Two layouts — full-card for primary list surfaces, `compact` inline strip for sidebar widgets — share the same `getMessage()` helper that prefers the server's `error.message` over the axios fallback. Wired into the 4 highest-traffic list pages ([approvals](apps/office-ui/src/app/office/approvals/page.tsx), [agents](apps/office-ui/src/app/office/agents/page.tsx), [integrations](apps/office-ui/src/app/office/integrations/page.tsx), [logs](apps/office-ui/src/app/office/logs/page.tsx)) and the 3 compact builtin widgets ([approvals](apps/office-ui/src/widgets/builtin/ApprovalQueueWidget.tsx), [activity](apps/office-ui/src/widgets/builtin/ActivityLogsWidget.tsx), [stats](apps/office-ui/src/widgets/builtin/DashboardStatsWidget.tsx)). The 401/403 axios interceptor still owns its lane — toast for 403, refresh-or-redirect for 401 — so this surface only renders for genuine fetch failures (5xx, 4xx other than 401/403, or network). Pages that haven't been converted yet (workspaces, settings, security, page-internal sub-fetches on the dashboard) keep their existing error handling — they get the same drop-in fix when next touched, no schema or API churn needed.
- [x] **office-ui** — Handle 401/403 redirects consistently. The axios response interceptor in [lib/api.ts](apps/office-ui/src/lib/api.ts) now centralises both: 401 still triggers refresh-token exchange, but the redirect-to-login path goes through a `redirectToLogin()` guard that no-ops when the caller is already on `/login`, `/register`, or `/forgot-password` — those pages issue session-probe requests that 401 by design, and the prior code reload-looped them back to themselves. 403 (authenticated but no permission) was previously unhandled and bubbled raw to React Query callers; it now surfaces a `toast.error('Permission denied', ...)` via the existing [useToastStore](apps/office-ui/src/store/useToastStore.ts), preferring the server's `error.message` when present and falling back to a generic copy. Redirecting to login on 403 is wrong (the user IS logged in, they just lack authz) so the toast-and-stay path keeps the React Query rejection intact for caller-specific UI without forcing a navigation. `isOnAuthPage()` is the single decision point — extending the unauthenticated-page list (e.g., a future `/sso-callback`) is a one-line addition.

### 3.2 Accessibility (WCAG 2.1) ✅
- [x] **office-ui** — Semantic landmarks (`<main>`, `<nav>`, `<header>` with roles) in layout
- [x] **office-ui** — ARIA labels on all interactive elements (buttons, inputs, links, modals)
- [x] **office-ui** — `aria-live="polite"` on chat messages, `role="log"` on message container
- [x] **office-ui** — `aria-invalid` + `aria-describedby` for form error states (login, register)
- [x] **office-ui** — `role="tablist/tab/tabpanel"` on agent wizard, `aria-current="step"` on steps
- [x] **office-ui** — `aria-current="page"` on active sidebar nav links
- [x] **office-ui** — Keyboard navigation for modals + dropdowns. New [lib/focusTrap.ts](apps/office-ui/src/lib/focusTrap.ts) helper installs a Tab/Shift+Tab handler that bounces focus inside a container's focusable descendants; modal components opt in via `useEffect`. Two-button modals (e.g. [ConfirmDialog](apps/office-ui/src/components/ui/ConfirmDialog.tsx)) use an inline trap (Tab cycles between cancel↔confirm). [CommandPalette](apps/office-ui/src/components/layout/CommandPalette.tsx) handles ↑↓/Enter/Esc inline (already shipped in §6.5); the trap utility is the reusable primitive for future modals. Sidebar collapse / hamburger were already keyboard-reachable from §3.2's first wave.
- [ ] **office-ui** — Test with screen reader (VoiceOver/NVDA) and fix issues. Manual QA — defer.
- [x] **office-ui** — Ensure minimum 44px touch targets on mobile. Enforced globally in [app/globals.css](apps/office-ui/src/app/globals.css) via a `@media (pointer: coarse)` rule that applies `min-height: 44px; min-width: 44px` to every `button`, `[role="button"]`, and `a[role="button"]` — matching WCAG 2.5.5 / Apple HIG / Material guidance. Scoping by pointer type leaves desktop spacing untouched (so the dense toolbars and small chrome elements like the 1.5-px scrollbar thumb don't grow), and opt-outs (`data-touch-exempt`, `data-inline`) are wired in for the rare cases where a tightly-packed inline control is already wrapped in a larger touch surface or used as a textual link. Single CSS layer rather than a per-component audit, so the site won't drift back below the floor as new components land.

### 3.3 Missing Pages & Flows ✅
- [x] **office-ui** — Implemented `/forgot-password` page (matching login theme, zod validation)
- [x] **office-ui** — Fixed dead link `/office/boards` → `/office/projects`
- [x] **office-ui** — SSO button now shows toast instead of alert
- [x] **office-ui** — Frontend SSO/OAuth wiring landed; runtime IdP config is the operator's job. New [useSsoLogin](apps/office-ui/src/hooks/useSsoLogin.ts) hook builds Keycloak's `/protocol/openid-connect/auth` URL with `kc_idp_hint=<alias>` — Keycloak handles every upstream OAuth handshake; the frontend only redirects. Login page's Google + GitHub buttons (Apple was a placeholder; GitHub is more useful for the audience) call through, plus the catch-all "Sign in with SSO" goes to Keycloak's IdP chooser (no `kc_idp_hint`). New [/login/callback](apps/office-ui/src/app/login/callback/page.tsx) page receives `?code=…&state=…`, posts to the token endpoint with `grant_type=authorization_code`, hydrates [useAuthStore](apps/office-ui/src/store/useAuthStore.ts) with the resulting access + refresh tokens, decodes JWT claims to populate the user profile (id, email, display_name, role), then routes to the `next` destination encoded in `state`. `force-dynamic` + a Suspense boundary keep the callback off the static-prerender path; failure path shows a "sign-in failed" panel + 4s auto-redirect to /login. **Operator action required:** Identity Providers → Add Provider in the Keycloak admin console for each alias (`google`, `github`); plug in the OAuth client ID/secret the upstream IdP issues; whitelist `${origin}/login/callback` as a Valid Redirect URI on the `urule-office` client. The frontend has no knowledge of any specific IdP — pluggable via Keycloak's existing UI.
- [ ] **office-ui** — Email verification flow after registration. Needs Keycloak's "Verify Email" required-action + an SMTP relay; same blocker as SSO.
- [x] **office-ui** — Logout confirmation dialog. New [components/ui/ConfirmDialog.tsx](apps/office-ui/src/components/ui/ConfirmDialog.tsx) — modal with title + description + cancel/confirm buttons, configurable `variant` (`destructive` paints confirm red), Escape cancels, Tab cycles between buttons (focus trap), backdrop-click cancels, autofocus on confirm so a quick Enter accepts the default. Wired into the Cmd+K palette's "Sign out" command — instead of immediate logout, opens the confirm with destructive styling. Reusable for future destructive actions (delete agent, uninstall package, revoke entitlement).

### 3.4 Loading States ✅
- [x] **office-ui** — Reusable Skeleton/SkeletonCard/SkeletonList components
- [x] **office-ui** — Skeleton loaders for chat list, agent catalog, dashboard stats, approvals, agent wizard
- [x] **office-ui** — Consistent skeleton pattern replacing all spinner-based loading states

### 3.5 Notification System (partial ✅)
- [x] **office-ui** — Toast notification component (success/error/warning/info) — done in 3.1
- [x] **office-ui** — Notification center (bell icon in header with notification history). New [store/useNotificationCenterStore.ts](apps/office-ui/src/store/useNotificationCenterStore.ts) (zustand+persist, namespace `urule-notification-center`, capped at 100 entries to keep localStorage bounded; per-entry kind/title/body/createdAt/read/href/source). New [components/layout/NotificationCenter.tsx](apps/office-ui/src/components/layout/NotificationCenter.tsx) — popover anchored to a bell button, listed newest-first, click to mark-read + route to `href` if set, mark-all-read + clear-all top-bar actions, hover-revealed dismiss button per entry, unread badge on the bell with `99+` overflow. New [hooks/useNotificationCapture.ts](apps/office-ui/src/hooks/useNotificationCapture.ts) mirrors every toast into the center so a user who missed the auto-dismissed toast can read it later. AppHeader's stub bell button replaced with `<NotificationBell>`; the center is mounted globally in office layout. Playwright spec [e2e/notification-center.spec.ts](apps/office-ui/e2e/notification-center.spec.ts) (5 specs) covers bell visibility, toggle, Escape close, empty state, seeded notification rendering, and clear-all.
- [x] **approvals + langgraph-adapter + office-ui** — Wire approval events to real-time notifications via WebSocket. End-to-end now: (1) [approvals/5794e12](https://github.com/urule-ai/approvals/commit/5794e12) publishes `APPROVAL_REQUESTED` / `APPROVAL_DECIDED` / `APPROVAL_ESCALATED` to NATS with the workspace-keyed payload (workspaceId, agentId, status, priority, riskLevel, title, action, decidedBy, decision, assignedTo). (2) [langgraph-adapter/9690b98](https://github.com/urule-ai/orchestrator-adapters/commit/9690b98) subscribes via [approval-broadcaster.ts](https://github.com/urule-ai/orchestrator-adapters/blob/main/langgraph-adapter/src/adapter/approval-broadcaster.ts) and fans out to a new workspace-keyed WS endpoint `GET /api/v1/ws/workspaces/:workspaceId` — separate from conversation channels so a chat-listening tab doesn't receive approval pushes (and vice versa). (3) [office-ui/useApprovalEvents.ts](apps/office-ui/src/hooks/useApprovalEvents.ts) connects the WS, parses `{ type: 'approval', topic, data }` envelopes, fires the existing `useNotificationCenterStore.add()` with `href: /office/approvals#:approvalId`, and invalidates the `["approvals"]` React Query keys so any open approvals list refetches the canonical state. Mounted once in [office/layout.tsx](apps/office-ui/src/app/office/layout.tsx) so the subscription lives for the whole authenticated session. Auto-reconnect with capped exponential backoff (500ms → 30s) covers transient drops. NATS unavailability is non-fatal at every hop — producer skips publish, broadcaster degrades to no-op, hook keeps trying — so the polling-based approvals page never breaks if the real-time path has issues. `severity = priority ?? riskLevel` chooses the toast `kind` (`critical`/`high` → warning, otherwise info; decision events distinguish approved=success / denied=error).

### 3.6 Theme & Visual ✅
- [x] **office-ui** — Light mode CSS variables + Tailwind mapped to CSS vars
- [x] **office-ui** — Theme toggle in Settings (Dark/Light/System cards)
- [x] **office-ui** — `useThemeStore` with Zustand persist
- [x] **office-ui** — Respects `prefers-color-scheme` via "System" option
- [x] **office-ui** — Flash-free theme init via inline script before hydration

### 3.7 Mobile UX ✅
- [x] **office-ui** — Collapsible sidebar with hamburger menu (`useSidebarStore`)
- [x] **office-ui** — Overlay backdrop on mobile, auto-close on nav link click
- [x] **office-ui** — Agent wizard: full-screen modal, stacked fields, compact step bar
- [x] **office-ui** — Chat: fixed input above keyboard, horizontal-scroll action pills
- [x] **office-ui** — Add bottom navigation bar for mobile. New [components/layout/MobileBottomNav.tsx](apps/office-ui/src/components/layout/MobileBottomNav.tsx) — fixed-bottom nav with the 5 most-used destinations (Home / Chat / Agents / Approve / More) as 56px tabs, visible only below the `lg:` breakpoint so the desktop sidebar layout is untouched. Mounted in [app/office/layout.tsx](apps/office-ui/src/app/office/layout.tsx) alongside `pb-16` on the main content area so the last row of any page isn't hidden behind the bar; the status-bar `WidgetZone` is hidden below `lg:` (a 32px slice would fight the bottom nav for the same thumb-reachable zone). Honours `env(safe-area-inset-bottom)` so the bar sits above the iOS home-indicator. Active-tab highlight echoes AppSidebar's `isActive` semantics so the same route lights up the same item across both navs, and the active icon switches to `icon-filled` for clearer touch feedback.

---

## 4. Infrastructure (Medium)

### 4.1 Database Migrations ✅
Replace fragile init scripts with proper versioned migrations.

- [x] **registry** — Generated `migrations/0000_brave_silver_surfer.sql` from the existing `src/db/schema/*.ts` files. drizzle-kit was bumped 0.24→0.31 and drizzle-orm 0.33→0.45 to make `db:generate` work; `drizzle.config.ts` at the service root drives both `db:generate` and `db:migrate`.
- [x] **packagehub** — Generated `migrations/0000_tense_frank_castle.sql` (packages + package_versions tables) the same way. Added `0001_pg_trgm.sql` (custom migration via `drizzle-kit generate --custom`) to capture the `pg_trgm` extension that the retired init script used to install for trigram-based search.
- [x] **mcp-gateway** — Generated `migrations/0000_damp_eddie_brock.sql` (mcp_servers + workspace_bindings + tools). Added `db:generate` and `db:migrate` to package.json (registry/packagehub already had them).
- [x] **infra** — Documented in [docs/MIGRATIONS.md](docs/MIGRATIONS.md): generation, application, rollback (forward-only — write a new migration that undoes the previous), testing, and the upgrade path from legacy init-script-bootstrapped DBs (drop volume *or* manually seed `drizzle.__drizzle_migrations`).
- [x] **infra** — Wired migrations as one-shot Compose services. New `migrator` multi-stage target in [services/registry/Dockerfile](services/registry/Dockerfile), [services/packagehub/Dockerfile](services/packagehub/Dockerfile), and [mcp-gateway/Dockerfile](../mcp-gateway/Dockerfile) ships drizzle-kit + the migrations dir + drizzle.config.ts and runs `npx drizzle-kit migrate`. New `registry-migrate` service in phase1.yaml + phase6.yaml, plus `packagehub-migrate` in phase6.yaml; long-running services depend on them via `service_completed_successfully`. Seed scripts (`seed-registry.sql`, `seed-packagehub.sql`) moved out of `docker-entrypoint-initdb.d` into one-shot `*-seed` services that run after each migrator (idempotent — `INSERT ... ON CONFLICT DO NOTHING`). Retired `init-registry-schema.sh` and `init-packagehub-schema.sh`. The `init-db.sql` (per-service `CREATE DATABASE`) stays — it's database creation, not schema, and doesn't belong in a migration. Compose configs validate (`docker compose config` clean for both phase1 and phase6). **Caveat**: live `docker compose build` of any image (existing `runner` stage and the new `migrator` stage alike) currently fails because the per-service `package-lock.json` files are stale and don't include the workspace deps (`@urule/auth-middleware`, `@urule/correlation-id`, `@urule/events`) that have been added to the service `package.json` files since the locks were last regenerated; `npm ci` then 404s on the registry workspace names. This is a pre-existing problem (the runner build was already broken before this work) and warrants its own ROADMAP item — see §5.x below.

### 4.1.1 Workspace deps in production Docker builds ✅
- [x] **infra (monorepo services)** — Pattern A: widened `build.context` to urule monorepo root for the 4 services in phase6 (registry, packagehub, state, office-ui). Each Dockerfile copies all workspace `package.json` files first, runs `npm ci` once at the workspace root, then builds the target workspace via `npm run build -w @urule/<service>`. Builder stage uses full devDeps; runner stage does its own `npm ci -w <svc> --omit=dev --include-workspace-root`. Migrator stage extends `deps` (full install) for direct drizzle-kit access. Compose contexts updated from `../../services/<x>` → `../..` with `dockerfile: services/<x>/Dockerfile`. Live-validated end-to-end: `registry-migrate` creates all 10 tables, `packagehub-migrate` creates 2 tables + the `pg_trgm` extension via the new 0001 migration, `*-seed` services populate 1 demo org + 27 demo packages, idempotent re-runs are no-ops.
- [x] **infra (standalones)** — Pattern B: widened `build.context` to `urule-repos/` (parent of urule and standalones) for langgraph-adapter and approvals. Their Dockerfiles COPY pre-built `dist/` of the consumed `@urule/*` packages directly (referenced via `file:` paths in package.json that resolve in-context). Pre-build at the host (`npm --prefix urule run build:all` + `npm --prefix orchestrator-contract run build`) is now a documented prerequisite for `docker compose build`. orchestrator-adapters happens to be its own npm workspace, so its Dockerfile reproduces enough of that workspace inside the image to let `npm ci` resolve the workspace tree.
- [x] **infra (.dockerignore)** — Added [urule/.dockerignore](.dockerignore) excluding `node_modules`, `*.tsbuildinfo`, `tests/`, `e2e/`, `*.test.ts`, `coverage/` etc. Keeps the monorepo build context lean (otherwise it ships the whole tree on every layer change).
- [x] **infra (remaining)** — channel-router, runtime-broker, mcp-gateway now in `docker-compose.phase6.yaml` with Pattern B Dockerfiles (`build.context: ../../..`). Each declares its OTel + prom-client deps directly in `package.json` rather than relying on `@urule/observability`'s transitive deps — Node ESM module resolution for file:-linked workspace packages walks up from the symlink target (not the linker), so transitives aren't found at the consumer level otherwise. Dockerfiles use `npm ci --install-links` so file:-linked packages are *copied* into node_modules (not symlinked), which lets npm resolve the dependency tree the way runtime expects. Lockfiles regenerated with `--install-links` to match. mcp-gateway also has a one-shot `mcp-gateway-migrate` Compose service (its drizzle schema). Live-validated end-to-end: all three build via `docker compose build`, come up healthy, expose `/metrics`, and Prometheus scrapes them as `up` targets.

### 4.2 Docker Improvements ✅
- [x] **All 12 Dockerfiles** — Added `HEALTHCHECK` instruction
- [x] **All services in compose** — `restart: unless-stopped` policy
- [x] **All services in compose** — Memory/CPU resource limits (512M services, 1G postgres, 256M nats)
- [x] **All services in compose** — Configured `json-file` log rotation with `max-size: 10m` / `max-file: 3` across `docker-compose.{infra,phase1,phase6}.yaml`. Each file declares its own `x-default-logging` anchor (compose `include:` doesn't propagate YAML anchors across files) and every service references it via `logging: *default-logging`. Validated with `docker compose config` on all three files.
- [x] **infra compose** — Added healthchecks to Temporal (via bundled `tctl namespace list`), Keycloak (`KC_HEALTH_ENABLED=true` exposes `/health/ready` on port 9000, with a fallback to `/` on 8080 for older builds), OpenFGA (`/healthz` on 8080) and OPA (`/health` on 8181). Devtools (otel-collector, jaeger) intentionally left without checks.

### 4.3 Structured Logging ✅
- [x] **All 11 services** — Enhanced Pino config with `LOG_LEVEL` env var, custom request serializer
- [x] **All services** — Request IDs via `crypto.randomUUID()` on every request
- [x] **All 4 error handlers** — Log errors with full context (err object, requestId, stack trace)
- [x] **All 10 services** — Correlation ID propagation across service boundaries. New `@urule/correlation-id` workspace package wraps a Fastify plugin (registered first, before CORS) that reads `x-correlation-id` from inbound headers, mints a ULID when absent, echoes it back on the response, overrides `request.id` (so pino auto-tags every log line), and stores it in `AsyncLocalStorage` so any code path can call `getCorrelationId()` zero-arg. Outbound calls use a `fetchWithCorrelation()` wrapper that injects the header from ALS — applied to the registry→adapter call site and all 16 in-ecosystem fetch sites in `langgraph-adapter`'s `anthropic-executor.ts` (external Keycloak/OPA/OpenFGA/manifest-loader calls intentionally left on plain `fetch`). `@urule/events` `EventBus.publish()` sets the header on the NATS message and falls back to ALS when no explicit `correlationId` is passed; `subscribe()` and `subscribeDurable()` re-establish the ALS context before invoking the handler so cascaded fetches in event consumers inherit the same ID. `office-ui`'s axios interceptor mints `x-correlation-id` per browser request so a user click is the actual root of the trace. New tests: `urule/packages/correlation-id/tests/plugin.test.ts` (11 tests covering inbound passthrough, mint-on-absent, ALS no-leak across parallel injections, fetch wrapper override behaviour), `event-bus-correlation.test.ts` (5 tests covering NATS roundtrip), and `tests/correlation-id.test.ts` per service (2 tests × 10 services).

### 4.4 OpenTelemetry & Tracing ✅
- [x] **registry** (and all services) — `@opentelemetry/sdk-node` instrumentation shipped via the new `@urule/observability` package's `initOtel(serviceName)` helper. Wired into every service's `package.json`. The auto-instrumentations cover HTTP servers + clients, `pg`, `dns`, `net` automatically — no manual span code required for the HTTP boundary.
- [x] **All services** — HTTP trace/span generation: covered automatically by `@opentelemetry/auto-instrumentations-node` once `initOtel()` is called.
- [x] **All services** — Cross-service propagation via W3C `traceparent`: also automatic with auto-instrumentation. Combined with the §4.3 `x-correlation-id` propagation, both human-friendly and trace-ID-shaped IDs flow end-to-end.
- [x] **All 10 services** — `initOtel(serviceName)` wired into each `src/index.ts`. Pattern: only `import { initOtel } from '@urule/observability';` is static (ESM imports are hoisted; SDK must start before Fastify resolves so auto-instrumentation can patch it). Everything else (`./config.js`, `./server.js`, etc.) is loaded via top-level `await import(...)` after `initOtel()` returns. SIGTERM/SIGINT handlers updated to call `await otelSdk?.shutdown()` so in-flight spans flush before container exit.
- [x] **infra** — OTEL Collector → Jaeger pipeline now flowing data end-to-end. Caught a config issue: the original [otel-collector-config.yaml](infra/compose/otel-collector-config.yaml) used the legacy `jaeger:14250` (jaeger.api_v2.CollectorService) endpoint, which modern jaeger releases removed in favour of OTLP-on-4317. Updated the `otlp/jaeger` exporter to point at `jaeger:4317`. Live-validated: registry traces visible in Jaeger UI at `localhost:16686`; service appears in `/api/services`; multiple GET spans returned by `/api/traces?service=registry`.

### 4.5 Database Performance ✅
- [x] **registry** — Indexes on agents(workspaceId, status), workspaces(orgId, slug), runtimes, providers, conversations
- [x] **packagehub** — Indexes on packages(name, type), versions(packageId)
- [x] **mcp-gateway** — Indexes on servers(name), bindings(workspaceId), tools(serverId)
- [x] **registry + mcp-gateway** — Pagination (limit/offset) on 5 list endpoints

### 4.6 Graceful Shutdown ✅
- [x] **All 11 services** — SIGTERM/SIGINT handlers calling `app.close()` + `process.exit(0)`
- [x] **langgraph-adapter** — Close WebSocket connections on shutdown. New `closeAllConnections()` in `src/routes/ws.routes.ts` walks the conversation→sockets map and sends a 1001 ("going away") close frame to every open client. The SIGTERM/SIGINT handler in `src/index.ts` now calls it (logging the closed count) before `app.close()`, so clients see an immediate disconnect instead of waiting for a TCP timeout.
- [ ] ~~**state** — Flush NATS KV state before shutdown~~ — N/A: the state service is currently in-memory only (`Map`-backed managers in `src/services/`); the CLAUDE.md description is aspirational. If/when state is migrated to NATS KV, the shutdown handler will need to flush. Tracked as part of that future migration, not as a graceful-shutdown gap.

---

## 5. Developer Experience (Medium)

### 5.1 Monorepo Tooling ✅
- [x] **urule** — Root `package.json` with npm workspaces (`packages/*`, `services/*`, `plugins/*`, `apps/*`)
- [x] **urule** — `npm run test:all`, `build:all`, `lint:all`, `typecheck:all` commands
- [x] **urule** — Turborepo for incremental builds + caching across the monorepo. New [turbo.json](turbo.json) defines a 6-task pipeline (`build`, `test`, `typecheck`, `lint`, `dev`, `clean`). `build` and `typecheck` cascade via `dependsOn: ["^build"]` so package consumers get their dependencies' `dist/` and `.d.ts` populated first; `test` and `lint` run flat. Cache outputs declared per task: `build` saves `dist/**` and `.next/**` (minus `.next/cache/**` — Next.js manages its own incremental cache there); `test` saves `coverage/**`. Root scripts now route through `turbo run <task>`; `:all`-suffixed aliases stay for back-compat with the existing CI workflow. Cold typecheck across 13 workspaces: 22s. Warm (cached): 140ms — `>>> FULL TURBO`. The change also unstuck several pre-existing rough edges: office-ui's `build` now uses `next build --no-lint` (the dedicated `lint` task is the source of truth for ESLint); office-ui's missing `typecheck` script alias added (was `type-check` only); `@hookform/resolvers` upgraded from 3.9 → 4.1.3 + zod from 3.23 → 4.3 (workspace alignment — the rest of the monorepo was already on zod v4 and the root hoist was shadowing office-ui's local v3, breaking its standalone typecheck), and the now-unnecessary `typescript.ignoreBuildErrors: true` removed from [next.config.mjs](apps/office-ui/next.config.mjs).

### 5.2 Shared Configurations ✅
- [x] **urule** — `tsconfig.base.json` shared TypeScript config
- [x] **urule** — `.eslintrc.json` shared ESLint config
- [x] **urule** — `.prettierrc` shared Prettier config
- [x] **urule** — `.nvmrc` pinned to Node 20

### 5.3 Dependency Alignment ✅
- [x] **All packages** — TypeScript aligned to `^5.5.0`
- [x] **All packages** — Vitest aligned to `^2.0.0`
- [x] **All packages** — Fastify aligned to `^5.0.0`

### 5.4 CI/CD Pipeline ✅
- [x] **urule** — GitHub Actions: lint + typecheck, test, security audit, Docker validation
- [x] **urule** — Issue templates (bug report, feature request)
- [x] **urule** — PR template with testing checklist
- [x] **urule** — Docker image build + push to GHCR on `v*` tag (matrix: registry, packagehub, state, office-ui). Uses `docker/build-push-action@v6` with GHA cache, `docker/metadata-action@v5` for semver+latest tagging. Builds `ghcr.io/urule-ai/<service>:<tag>` plus `:major.minor` and `:latest`.
- [x] **approvals, orchestrator-adapters (langgraph)** — Pattern B: GHA workflow checks out urule (and orchestrator-contract for langgraph) alongside the standalone, runs `npm run build:all` host-side to populate `dist/`, then `docker build` with parent-of-both as build context. Triggered on `v*` tag in the standalone repo.
- [x] **All standalone repos** — CI workflows already configured for build+test
- [x] **channel-router, runtime-broker, mcp-gateway** — docker-publish workflows shipped in each repo's `.github/workflows/ci.yml`. Pattern B style: triggered on `v*` tag, checks out urule alongside the standalone, runs `npm --prefix urule run build:all` to populate workspace dist/, then docker-builds with `context: .` and `dockerfile: <repo>/Dockerfile`. Pushes `ghcr.io/urule-ai/{channel-router,runtime-broker,mcp-gateway}:<tag>` plus `:major.minor` and `:latest`. Build-and-test job also updated to use `--install-links` and clone urule for workspace deps.

### 5.5 API Documentation ✅
- [x] **All 11 services** — `@fastify/swagger` + `@fastify/swagger-ui` with OpenAPI 3.0 specs
- [x] **All services** — Swagger UI at `/docs` (public, no auth required)
- [x] **All services** — Service-specific titles, descriptions, and tags
- [x] **All services** — Add route-level JSDoc/schema annotations for richer docs. **Landed in 5 in-repo services**: packagehub (19 routes, 7 tag groups), packages (7 routes, 2 tag groups), governance (4 routes, 3 tag groups), state (23 routes, 5 tag groups), registry (60 routes, 10 tag groups). Total **113 routes annotated** across all in-repo services. Every route gets a Fastify `schema: { tags, summary, description }` that swagger-ui surfaces directly; descriptions document error codes, idempotency rules, auth shape, and inter-route relationships rather than restating the URL. Verified end-to-end via `/docs/json` for each service — global tag arrays carry one-line section headers, every route except `/healthz` gains a summary + description. The body / params / querystring upgrade also shipped via [`fastify-type-provider-zod@^6`](https://github.com/turkerdev/fastify-type-provider-zod) — every Zod schema that previously ran inside a manual `safeParse()` block is now lifted onto the route's `schema.body` / `schema.params` / `schema.querystring`, so swagger-ui surfaces real request shapes (not just descriptions) and Fastify validates inbound bodies via the type provider. The `safeParse()` boilerplate was removed across packages, packagehub, state, registry (governance had no Zod before — schemas were added net-new). Each service's error handler now maps `hasZodFastifySchemaValidationErrors`-detected failures back to the historical `{ error: 'Validation failed', details }` envelope so existing API consumers + tests don't see the type-provider's default Fastify error shape. State was bumped from `zod@^3.23` to `zod@^4.3.6` to align with the rest of the workspace + meet the type provider's `>= 4.1.5` peer; the only Zod 4 syntax change was `z.object({}).passthrough()` → `.loose()` in two widget-state schemas. **Verified counts (post-upgrade)**: registry 14 body schemas + 34 path-params + 13 query-params across 62 routes, packagehub 7 body schemas + 20 path-params + 13 query-params across 20 routes, state 11 body schemas + 32 path-params + 2 query-params across 23 routes, packages 2 body schemas + 6 path-params across 7 routes, governance 4 body schemas across 4 routes. All 243 tests across the five services still pass; typecheck + build clean. **Standalone-repo fan-out (also shipped)**: same uniform pattern applied to `runtime-broker` (4 routes, 1 safeParse), `channel-router` (10 routes, 5 safeParse, zod 3→4 bump), `mcp-gateway` (13 routes, 3 safeParse), `langgraph-adapter` (10 routes, 7 safeParse), and `approvals` (15 routes, 7 safeParse, zod 3→4 bump) — 52 additional routes covered, bringing the total to **165 routes annotated + Zod-validated** across all 10 services. The two zod 3→4 bumps (channel-router, approvals) needed only `.passthrough()` → `.loose()` syntax fixes; everything else (`z.object`, `z.string`, `z.array`, `z.enum`, `.min()`, `.optional()`) is stable across the major version. Each standalone repo's tests pass on the new wiring: runtime-broker 16, channel-router 43, mcp-gateway 29, langgraph-adapter 41, approvals 62 — **191 tests across the 5 standalones, all green**. Per-repo error handlers detect Zod failures via `hasZodFastifySchemaValidationErrors` and re-emit the historical `{ error: 'Validation failed', details }` envelope so external API consumers + cross-repo tests don't see a wire-format change. Section §5.5 is now closed.
- [x] **Libraries** — TypeDoc auto-generated API documentation. Root [typedoc.json](typedoc.json) drives an `entryPointStrategy: 'packages'` build over the six published libraries (`auth-middleware`, `authz`, `correlation-id`, `events`, `observability`, `spec`); per-package [typedoc.json](packages/auth-middleware/typedoc.json) shims point each at `src/index.ts` so the generator reads source rather than compiled `dist/`. New `npm run docs:api` produces a single static site at `docs/api/` covering every public type/function/class with cross-package linking; `docs:api:watch` rebuilds incrementally during doc-writing. Output is gitignored — every CI run reproduces it deterministically. New [docs/API.md](docs/API.md) is the human-facing entry point: how to generate, how to add a new package, hosting notes, troubleshooting. The 0-error / 3-warning baseline catches genuine leaks (e.g., `AuditActor` referenced from `AuditLogger.accessDenied.actor` but not exported from `@urule/events`'s public surface — follow-up `export type` add).

### 5.6 Developer Setup ✅
- [x] **urule** — `scripts/dev-setup.sh` with prerequisite checks and guided setup
- [x] **urule** — `Makefile` with `make dev`, `make test`, `make build`, `make infra-up/down`, `make e2e`, `make clean`
- [x] **urule** — `scripts/clone-all.sh` clones all standalone repos

---

## 6. Features (Low)

### 6.1 Widget System
- [x] **widget-sdk / office-ui** — Widget configuration persistence (save widget settings). Backend was already in place — `state` service exposes `GET/PUT/PATCH/DELETE /api/v1/widget-state/:instanceId` ([state.routes.ts](services/state/src/routes/state.routes.ts)) backed by [WidgetStateManager](services/state/src/services/widget-state-manager.ts). Frontend gap closed with new [useWidgetConfig](apps/office-ui/src/widgets/useWidgetConfig.ts) hook exposed from [widgets/index.ts](apps/office-ui/src/widgets/index.ts) — call site looks like `const { config, update, reset } = useWidgetConfig({ refreshInterval: 30 })` and the rest is automatic. On mount, the hook GETs the persisted state (404 → fall back to defaults + the manifest's `defaultConfig`). Every `update(patch)` does an optimistic in-memory merge AND debounces a 400ms PATCH so a settings panel's on-change cascade coalesces to one network call. First save lazily upgrades the missing-row 404 to a PUT (creates the row); a stale-row 404 on subsequent PATCHes auto-falls-back to PUT too, so deleting state out from under a widget doesn't leave it stuck. Pending writes flush on unmount via the cleanup callback so quick dismiss-then-remount flows don't lose the most recent edit. Widget instance id is the single key — `widget.widgetId` is the same identifier the state service expects. No new backend code, no schema changes; the §6.1 widget-system box gains its first user-visible "remembers what I configured" feature.
- [x] **widget-sdk** — Widget-to-widget communication protocol landed in [widget-sdk@f7eb2b6](https://github.com/urule-ai/widget-sdk/commit/f7eb2b6). New `PeerMessageBus` class (one per host page) routes `widget:peer-send` events between iframe-hosted widgets and re-emits them as `host:peer-message` on the destination iframe(s). Client API: `clientBridge.broadcastToPeers(topic, data)`, `clientBridge.sendToPeer({ target: 'widget-id', topic, data })`, `clientBridge.onPeerMessage(cb)` (receives `{ from, topic, data }`). Sender identity is unforgeable: the bus matches `event.source` against registered iframe `contentWindow`s and stamps `from` with the *registered* widgetId; `msg.widgetId` from the event is never trusted. Targets are either a specific widgetId or `'*'` for broadcast (excludes the sender). Iframe-only scope; native (in-process) widgets share React state through the host already and don't need the bridge. Six new tests cover registration tracking, broadcast / targeted / unknown / topic-less paths, plus the unforgeable-`from` property. 23 → 29 widget-sdk tests pass.
- [x] **office-ui** — Make widgets truly modular (was: most are page re-exports). Three of the five page-reexport widgets converted to compact, sidebar-sized components: [DashboardStatsWidget](apps/office-ui/src/widgets/builtin/DashboardStatsWidget.tsx) renders a 2×2 KPI tile grid backed by `/office/stats`; [ApprovalQueueWidget](apps/office-ui/src/widgets/builtin/ApprovalQueueWidget.tsx) shows the top-N pending items with risk-dot + relative timestamp, deep-linking each row to `/office/approvals#:id` rather than embedding the page's filter chrome and decision workflow; [ActivityLogsWidget](apps/office-ui/src/widgets/builtin/ActivityLogsWidget.tsx) is the same shape for `/logs`. Each uses the new [useWidgetConfig](apps/office-ui/src/widgets/useWidgetConfig.ts) hook so per-instance `limit` (1..20) is persisted via the state service. The two remaining page-reexports — [IntegrationsWidget](apps/office-ui/src/widgets/builtin/IntegrationsWidget.tsx) and [ProjectsWidget](apps/office-ui/src/widgets/builtin/ProjectsWidget.tsx) — wrap pages whose compact variant needs more product input (which subset of integrations/projects matters as a tile?) and are tracked as a follow-up below. Pattern is now established: `useQuery` + `Skeleton` + `useWidgetConfig` for any future widget; the page-reexport anti-pattern can stay flagged in code review.
- [x] **office-ui** — Widget drag-and-drop customization in the dashboard. New [useDashboardLayoutStore](apps/office-ui/src/store/useDashboardLayoutStore.ts) (zustand + persist) holds an ordered widget-id array per `${workspaceId}::${mountPoint}` key — only the order is stored; the actual instances stay in the in-memory registry. Order resolution prefers the persisted entry, then falls back to the registry's natural sort. New instances added after a layout is saved appear at the end (no reordering required); persisted ids whose instances no longer exist are pruned at read time. [WidgetZone](apps/office-ui/src/widgets/WidgetZone.tsx) takes a new `reorderable` prop — when set + `editing` is on, every child gains an HTML5-DnD drag handle and acts as a drop target (inline impl, no extra dep — fine for the ~10-tile sidebar/dashboard surfaces). Visual feedback: dragged item dims to 40%, drop target gets a 2px primary outline. Reorder writes the new id list to the store on drop. New "Customize" toggle in [AppHeader](apps/office-ui/src/components/layout/AppHeader.tsx) flips global edit mode (icon + label change to "Done" while editing). Edit state is intentionally NOT persisted — on the next session the user expects a read-only dashboard, not a leftover toolbar. Sidebar zone is the first reorderable mount-point; dashboard `main-panel` and other zones can opt in by adding `reorderable` once their layout is stable.
- [x] **office-ui** — Widget marketplace UI (browse, install, configure). Cheap path: widgets are now a packagehub package `type: 'widget'` (no schema change — `package.type` is `varchar(50)`). Browse: [marketplace browse page](apps/office-ui/src/app/office/marketplace/page.tsx) gains a Widget filter pill alongside personality / skill / mcp_connector / design — same chrome, same search, same verified+sort affordances. Detail: [marketplace detail page](apps/office-ui/src/app/office/marketplace/%5Bname%5D/page.tsx) detects `pkg.type === 'widget'` and forks the install handler — instead of POSTing to the packages-service `/install` lifecycle (which is for personality/skill/etc that need server-side install state + entitlement checks), it pulls the embedded `WidgetManifest` out of `pkg.latestVersion.manifest.widget`, persists it into the new [useInstalledWidgetsStore](apps/office-ui/src/store/useInstalledWidgetsStore.ts) (zustand+persist, namespace `urule-installed-widgets`, keyed by workspaceId), and registers it into the in-memory `widgetRegistry`. The widget appears on the dashboard / sidebar / wherever its `mountPoints` declare on the next render. Configure: existing [`useWidgetConfig`](apps/office-ui/src/widgets/useWidgetConfig.ts) hook handles per-instance settings via the state service, no change required. Boot-time hydration in [providers.tsx](apps/office-ui/src/app/providers.tsx)'s `useWidgetRegistryInit` walks `useInstalledWidgetsStore.byWorkspace` and registers every persisted manifest, so installed widgets survive a page refresh. Manifest convention documented in [marketplace-api.ts](apps/office-ui/src/lib/marketplace-api.ts) — packagehub package's `manifest.widget` field carries a `WidgetManifest` JSON; `extractWidgetManifest()` validates the shape (id / name / version / description / author / mountPoints / entryType + entryUrl-required-for-external / permissions / defaultConfig) before persisting, so a malformed package can't poison the registry. Idempotent install (re-clicking dedups by manifest id; same handler path also handles version bumps cleanly). **What's deliberately out-of-scope for this iteration**: (1) server-side per-workspace `installed_widgets` table — today the store is localStorage-only, so installed widgets are per-browser-session; cross-device parity needs the registry-service follow-up; (2) the detail-page CTA state machine doesn't yet read from `useInstalledWidgetsStore`, so the button stays "INSTALL FOR FREE" after a widget install (re-clicking is idempotent — the store dedups — but the visual signal is missing); (3) a marketplace-publishing flow for widget packages from the office-ui side (publishers push via the existing CLI/API for now); (4) sample seed widget packages in [seed-packagehub.sql](infra/compose/seed-packagehub.sql) — the install path works given a properly-shaped package, but no canonical demo widget exists yet. Typecheck + Next.js build clean.
- [x] **office-ui** — Remaining page-reexport widgets converted. Picked sensible defaults instead of waiting for product input: [IntegrationsWidget](apps/office-ui/src/widgets/builtin/IntegrationsWidget.tsx) defaults to `focus: 'attention'` — surfaces disconnected / needs-attention rows first (the "is anything broken?" tile) with a per-config flip to `focus: 'active'` for last-synced view; [ProjectsWidget](apps/office-ui/src/widgets/builtin/ProjectsWidget.tsx) defaults to `focus: 'at_risk'` — at-risk → active → planning sort with a tiny per-row progress bar, color-coded by status; per-config flip to `focus: 'active'` orders by `updated_at`. Both honour the `limit` knob (1..20 / 1..12) and use `useQuery` + `Skeleton` + `useWidgetConfig` exactly like the three widgets converted alongside the §6.1 modularity work — pattern is now uniform across all 5 builtin widgets and the page-reexport anti-pattern is gone.

#### Widget marketplace follow-ups (open)
- [ ] **registry** — Server-side `installed_widgets` table backing the marketplace install flow so widgets follow a workspace across devices. Today's [useInstalledWidgetsStore](apps/office-ui/src/store/useInstalledWidgetsStore.ts) is localStorage-only — installing a widget on a laptop doesn't propagate to the same workspace on a phone. Add `POST/GET/DELETE /api/v1/workspaces/:wsId/installed-widgets` (workspaceId + manifestJson + installedAt) and have the zustand store fall through to it with localStorage as a cache layer.
- [ ] **office-ui** — Marketplace detail page CTA state machine reads from `useInstalledWidgetsStore` so a widget package's button reflects "INSTALLED" / "UPDATE AVAILABLE" / "UNINSTALL" instead of staying on "INSTALL FOR FREE" forever. Re-clicking is idempotent today (the store dedups by manifest id), but the visual signal is wrong.
- [ ] **office-ui** — Widget publishing flow (UI for authoring + pushing a widget package to packagehub). Today publishers use the CLI / HTTP API directly; a UI path with manifest validation + dry-run preview lowers the barrier.
- [ ] **infra** — Sample seed widget package in [seed-packagehub.sql](infra/compose/seed-packagehub.sql) so a fresh `make dev` boot has at least one third-party widget the marketplace can install — closes the "no canonical demo widget" follow-up flagged in the §6.1 marketplace entry above.

### 6.2 Agent Capabilities
- [x] **registry** — Implemented agent memory storage. New `agent_memories` table (id ULID, agent_id FK→agents ON DELETE CASCADE, content text ≤10000, kind varchar default 'note', tags jsonb default [], created_at). Drizzle migration `0001_agent_memories.sql` generated. GET / POST / DELETE handlers with Zod validation, 404 on missing agent or memory, pagination via `limit`/`offset` (max 100). Replaces the 3 stub handlers in `agents.routes.ts`.
- [x] **registry** — Implemented real agent metrics derived from `messages` and `conversation_agents` source-of-truth. `GET /agents/:id/metrics` returns `messages_sent`, `messages_sent_24h`, `conversations_participated`, `last_active`. CPU/memory fields kept at 0 with a comment — agents are logical actors, not OS processes; surfacing fake numbers was misleading. New `messages_sender_id_idx` (sender_id, sender_type) index supports the aggregate queries. Returns 404 for unknown agentId. `tasks_completed`/`tasks_in_progress` removed — those live in state service; if office-ui needs them it should call state directly.
- [x] **registry** — Implemented real agent health checks. `GET /agents/:id/health` derives `status` from message activity: `'healthy'` (<5min), `'idle'` (<30min), `'stale'` (>30min), `'never_active'` (no messages ever). `last_heartbeat` is the most recent message timestamp. Same memory/CPU caveat as metrics. Returns 404 for unknown agentId.
- [x] **`@urule/llm-providers` shared lib + langgraph-adapter refactor** — Multi-provider LLM streaming abstraction landed as a new workspace package at [packages/llm-providers/](packages/llm-providers/). Provider-agnostic types in [src/types.ts](packages/llm-providers/src/types.ts) (`LlmProvider`, `LlmStreamParams`, `LlmMessage`, `LlmToolDefinition`, `LlmToolCall`, `LlmEvent`); two impls ship — [AnthropicProvider](packages/llm-providers/src/anthropic-provider.ts) wraps `@anthropic-ai/sdk`'s streaming API + groups tool results onto a single user message per Anthropic's protocol, and [OpenAIProvider](packages/llm-providers/src/openai-provider.ts) wraps the `openai` SDK + reassembles tool-call deltas (OpenAI streams arguments incrementally; Anthropic only delivers tool_use blocks on the final message). [createProviderRegistry](packages/llm-providers/src/registry.ts) provides `get(name)` / `list()` lookup; later registrations of the same name win, so a deployment can swap a default impl for a test mock without forking. 16 tests cover the registry + every event-shape branch on both providers (text streaming, tool call assembly, malformed-JSON tool args, error path). [orchestrator-adapters/langgraph-adapter@feat](https://github.com/urule-ai/orchestrator-adapters) refactored to consume the shared lib: `AnthropicExecutor` (name kept for back-compat) now takes an `LlmProviderRegistry` constructor arg, picks the impl via `agentConfig.provider_id` → `providerKey.provider` (with `claude`/`anthropic`/`openai`/`gpt` aliases), and replaces the direct `anthropic.messages.stream()` call with `provider.streamChat(...)`. The existing tool-call flow (hire_agent / create_task / update_task_status, action buttons) is unchanged. All 41 adapter tests still pass; typecheck + build clean. **Why a shared lib instead of internal**: future CrewAI / AutoGen / ADK adapters import this package and inherit anthropic + openai support for free — no duplicated provider plumbing per adapter. Adding Gemini or a local Ollama provider is one new file in `@urule/llm-providers` consumed by every adapter.
- [x] **registry** — Conversation branching / forking. Schema additions in [conversations.ts](services/registry/src/db/schema/conversations.ts) (`parent_conversation_id` + `branched_from_message_id`, both nullable, no FK so a parent delete leaves the child as a top-level orphan rather than cascading) + a `conversations_parent_idx` for tree-walks. Migration [0002_conversation_branching.sql](services/registry/migrations/0002_conversation_branching.sql) is pure additive. Two new endpoints in [conversations.routes.ts](services/registry/src/routes/conversations.routes.ts): `POST /api/v1/conversations/:id/branch` (body `{ fromMessageId, title?, agentIds? }`) creates a new conversation linked to the parent and copies every message up to and including `fromMessageId` into the branch with fresh ULIDs (so subsequent edits in either branch are independent); inherits the parent's agent participants unless `agentIds` is set. `GET /api/v1/conversations/:id/branches` lists immediate children newest-first (deeper trees iterate). Title defaults to "<parent.title> (branch)". 404 on missing parent or message; 400 when `fromMessageId` belongs to a different conversation. Branching lives in the registry rather than an adapter because registry owns conversations + messages — adapters that don't know about forks keep working unchanged. 8 new tests; 30 → 38 registry tests pass.
- [x] **orchestrator-contract + adapters** — Fixed `pauseForApproval` ID mismatch. `ApprovalRequest` now carries an optional `id`; both langgraph-adapter and goose-adapter (and the contract's mock-adapter) use it when provided so `resumeRun({approvalId})` can target a specific pending approval. Compliance suite extended with a roundtrip test.
- [x] **orchestrator-adapters** — Add CrewAI, AutoGen, and ADK adapters as new workspace packages in the [orchestrator-adapters](https://github.com/urule-ai/orchestrator-adapters) monorepo (alongside `goose-adapter` and `langgraph-adapter`). All three shipped as `0.1.0-spike.0` workspace packages, each implementing the 8-method `OrchestratorAdapter` from `@urule/orchestrator-contract` over an in-memory client (production sidecars deferred behind a `*Client` interface seam — same posture as `goose-adapter`'s spike). **CrewAI** ([crewai-adapter](https://github.com/urule-ai/orchestrator-adapters/tree/main/crewai-adapter)): Urule agent → CrewAI **Crew config**, Urule run → CrewAI **kickoff**; config fields `crewConfigPath` / `crewInline` / `process` (`sequential` or `hierarchical`) / `provider` / `model` / `mcpBindings`. **AutoGen** ([autogen-adapter](https://github.com/urule-ai/orchestrator-adapters/tree/main/autogen-adapter)): Urule agent → AutoGen **agent config**, Urule run → AutoGen **conversation/run**; config fields `agentConfigPath` / `agentInline` / `chatPattern` (`single` / `group` / `nested` / `swarm`) / `provider` / `model` / `mcpBindings`. **ADK** ([adk-adapter](https://github.com/urule-ai/orchestrator-adapters/tree/main/adk-adapter)): Urule agent → ADK **agent config** (model + tools + optional sub-agents), Urule run → ADK **invocation**; config fields `agentConfigPath` / `agentInline` / `agentType` (`llm` / `sequential` / `parallel` / `loop`) / `provider` / `model` / `mcpBindings`. All three drop unknown enum values rather than passing them through to the sidecar (which would 400 on unknown kinds). All three lift Zod schemas onto `schema.body` / `schema.params` from day one (uniform with the rest of the platform), use Zod 4 syntax (`.loose()` not `.passthrough()`), and ship a Zod-aware error handler that re-emits the `{ error: 'Validation failed', details }` envelope. Each adapter has 22 tests (8 contract-compliance + 8 adapter unit + 6 routes smoke) covering config forwarding, inline-definition passthrough, invalid-enum-value drop, run completion → RunState, JSON.stringify fallback, pause/resume approval tracking, unknown-runId rejection, and capabilities. **Full monorepo gate**: `npm run typecheck:all` + `npm run test:all` green across all 5 adapters — adk 22, autogen 22, crewai 22, goose 19, langgraph 41 = **126 tests across the orchestrator-adapters monorepo**, all passing. Real sidecar impls (Python subprocess or HTTP-to-sidecar for each) deferred behind their respective `*Client` interface seams — explicitly part of the spike posture, mirrors goose. Section §6.2 is now closed.
- [x] **governance** — Removed all `(request as any)`/`(decision as any)`/`(result as any)` casts in `src/routes/governance.routes.ts`. Request user access goes through a typed `getUser()` helper that mirrors auth-middleware's intersection-cast pattern with the exported `UruleUser` type. The decide/policy/authz return values are now read via their typed `.allowed` field directly. As a side effect, the audit log message for `/decide` now correctly shows `"allowed"`/`"denied"` (previously always `"evaluated"` because the cast hid that the return shape has no `.decision` field).
- [x] **governance** — Audit emit failures are no longer silenced. Each `.catch(() => {})` now logs at `request.log.warn` with the route name and underlying error, so emit failures show up in service logs alongside the request that caused them.

#### Adapter + LLM-provider follow-ups (open)
- [ ] **orchestrator-adapters** — Real `CrewaiClient` impl. Today's [`InMemoryCrewaiClient`](https://github.com/urule-ai/orchestrator-adapters/blob/main/crewai-adapter/src/crewai/crewai-client.ts) covers the spec but doesn't actually run a crew. Production wiring: Python subprocess driving the `crewai` CLI OR a long-lived HTTP-to-Python sidecar (FastAPI wrapping the `Crew` class). The `CrewaiClient` interface seam is already in place; only one new file lands behind it. Same shape applies to **AutoGen** (subprocess/sidecar driving `autogen-agentchat`) and **ADK** (subprocess/sidecar driving `google-adk`) — each is a separate iteration when concrete demand for that orchestrator surfaces.
- [ ] **orchestrator-adapters** — Real `GooseClient` impl. Today's `InMemoryGooseClient` is the only one shipped despite goose-adapter being marked spike-stable. Production wiring: subprocess driving `goose-cli` daemon, OR HTTP via Goose's Agent Client Protocol. Same `GooseClient` interface seam.
- [ ] **packages/llm-providers** — Add `GeminiProvider` (Google's `@google/generative-ai` SDK) + `OllamaProvider` (local-first via `ollama` SDK or its HTTP `/api/chat` endpoint). Both fit the existing `LlmProvider` shape; AnthropicProvider + OpenAIProvider are the templates. Once landed, every adapter that consumes `@urule/llm-providers` (langgraph today; future crewai/autogen/adk) gets them for free.
- [ ] **packages/llm-providers** — Opt-in integration tests against the real Anthropic + OpenAI APIs. Today's 16 tests use mocks. Add a separate test file gated by `RUN_LLM_INTEGRATION=true` so regular CI runs don't pay the $ cost or flake on upstream changes; the integration suite catches breaking-change-type signals (new event types, deprecated fields) at the cost of one paid run per release.
- [ ] **infra** — Compose service entries for `crewai-adapter`, `autogen-adapter`, and `adk-adapter` in [docker-compose.phase6.yaml](infra/compose/docker-compose.phase6.yaml). Pattern B Dockerfiles match the langgraph + goose adapters. Lands once the spike adapters get their real client impl (above) — running an adapter that only proxies to an in-memory client wastes a container.

### 6.3 Package Ecosystem
- [x] **packagehub** — Cryptographic package signing. New `publisher_pubkey` + `pubkey_kind` columns on `packages`; `signature` + `signature_kind` + `signed_at` on `package_versions`. Verifier in [services/packagehub/src/services/signing.ts](services/packagehub/src/services/signing.ts) uses Node built-in `crypto.verify(null, digest, pubkey, sig)` against an Ed25519 SPKI-wrapped raw 32-byte key — no external deps. Canonical digest: `SHA256(JSON.stringify(manifest, sortedKeys) || readme || version)`. Once a package is published with a publisher pubkey, every subsequent version publish must carry a signature that verifies against it (400 SIGNATURE_REQUIRED if missing, 401 SIGNATURE_INVALID if wrong). Anonymous packages stay unsigned for back-compat. New `GET /api/v1/packages/:name/versions/:version/verify` endpoint for consumers to sanity-check before installing. **Why Ed25519 first** (vs GitHub Sigstore OIDC, vs C2PA): Node has it built-in, works for CLI publishes from a laptop without platform lock-in, ergonomic for hobbyist authors. Sigstore OIDC is a planned follow-up — adds `pubkey_kind: 'sigstore-oidc'` dispatch to a Fulcio + Rekor verifier for repos that publish via a GitHub Actions workflow.
- [x] **packagehub** — Optional freemium / paid packages (marketplace foundation). New columns on `packages`: `license_tier` (`free`/`paid`/`subscription`, default `free`), `price_cents`, `payment_provider` (`stripe`/`lemonsqueezy`), `payment_link`. New `entitlements` table — `packageId` FK + (`workspaceId` OR `userId`) + `kind` (`purchase`/`subscription`/`grant`) + `externalRef` (idempotency key for webhooks) + `expiresAt`. New routes in [services/packagehub/src/routes/entitlements.routes.ts](services/packagehub/src/routes/entitlements.routes.ts): `GET /api/v1/entitlements?packageName=&workspaceId=` (the install-time gate; `free` short-circuits without a table lookup), `POST /api/v1/entitlements` (idempotent on `externalRef` — wire-compatible with Stripe webhooks), `DELETE /api/v1/entitlements/:id` (refunds). Default `free` keeps the open-source path; authors opt in by setting a different tier at publish.
- [x] **packages** — Entitlement check in install path. [services/packages/src/services/package-manager.ts](services/packages/src/services/package-manager.ts) `install()` now calls packagehub's `/api/v1/entitlements` *before* loading the manifest; throws `EntitlementRequiredError` (HTTP 402 Payment Required + `paymentLink` in the body) when the consumer has no entitlement for a non-free package. Transient packagehub outages don't block install (let the manifest-loader produce the clearer downstream error).
- [x] **packages** — Rollback capability for package upgrades. New `POST /api/v1/packages/:installId/rollback` endpoint backed by per-installation in-memory version history stack: `install()` seeds the stack with the freshly-installed version; `upgrade()` pushes the new version; `rollback()` pops to the previous one. Stack-of-1 returns HTTP 404 (`NO_HISTORY`). Persisting history to a Drizzle table is a follow-up — today's history resets across service restarts.
- [x] **packagehub** — Package ratings and reviews. New `package_reviews` table ([db/schema/reviews.ts](services/packagehub/src/db/schema/reviews.ts)) — id ULID + packageId FK ON DELETE CASCADE + reviewerId (Urule user) + rating (1-5 integer) + title (≤200 chars) + body (≤10K chars, default '') + optional version + timestamps. UNIQUE(packageId, reviewerId) constraint blocks duplicate submissions at the database level so concurrent POSTs can't race past the application check. Drizzle migration [0003_reviews.sql](services/packagehub/migrations/0003_reviews.sql). Four endpoints in [routes/reviews.routes.ts](services/packagehub/src/routes/reviews.routes.ts): `GET /api/v1/packages/:name/reviews` (paginated newest-first list + aggregate `{averageRating, reviewCount}` summary computed via SQL `AVG(rating)` + `COUNT(*)`), `POST` (insert with Zod validation; 409 `REVIEW_EXISTS` on UNIQUE-constraint violation pointing the caller at PATCH instead), `PATCH /:reviewId` (partial update via the `updatedAt` bump pattern; 400 if no fields provided), `DELETE /:reviewId` (204 on success). Listing supports `reviewerId` filter for "my reviews" views and `limit`/`offset` pagination capped at 100. 13 new tests in [tests/reviews.test.ts](services/packagehub/tests/reviews.test.ts) cover validation, 404 paths, happy paths, and the duplicate-reviewer 409 dispatch. Authorisation cross-check landed: POST rejects with 403 `REVIEWER_MISMATCH` when the body's `reviewerId` doesn't match `request.uruleUser.id` (anti-impersonation); PATCH and DELETE pre-fetch the existing row and return 403 `NOT_REVIEW_OWNER` when the caller didn't write it (so "the review exists but isn't yours" is distinguishable from "no such review"). 401 `UNAUTHENTICATED` on every mutating route when auth-middleware didn't decorate a user. 3 new tests added to [tests/reviews.test.ts](services/packagehub/tests/reviews.test.ts) (POST impersonation rejection, PATCH/DELETE on another user's review).
- [x] **packagehub** — Package dependency tree visualization. New [services/dependency-tree.ts](services/packagehub/src/services/dependency-tree.ts) — recursive walk over `manifest.dependencies` with per-path cycle detection (a `Set<string>` of ancestors prevents `root → child → root` infinite recursion while still letting two siblings share a transitive dep), latest-non-yanked resolution at each step, hard-cap recursion via configurable `maxDepth` (default 8). Each tree node carries `name` + `versionRange` (verbatim from parent manifest, forward-compat for proper semver-range matching later) + `resolvedVersion` (latest non-yanked match in packagehub, or null) + nested `dependencies` array, plus an optional `unresolved` reason (`missing` / `no_version` / `cycle` / `max_depth`) so consumers render unresolvable nodes differently. New `GET /api/v1/packages/:name/versions/:version/dependency-tree?maxDepth=N` endpoint in [routes/dependency-tree.routes.ts](services/packagehub/src/routes/dependency-tree.routes.ts) — 404 if root pkg/version missing, 400 if maxDepth out of [1,16]. 9 new tests in [tests/dependency-tree.test.ts](services/packagehub/tests/dependency-tree.test.ts) cover null-root, null-version, leaf, one-level, missing-transitive, yanked-only, root-↔-child cycle, sibling-shared-no-cycle, and maxDepth cutoff. Floor uses "latest non-yanked" instead of true semver-range matching — when `semver.satisfies` lands as a follow-up, only `services/dependency-tree.ts` changes.
- [x] **packages** — Auto-update notifications. New `PackageManager.checkUpdates(workspaceId)` ([services/package-manager.ts](services/packages/src/services/package-manager.ts)) iterates each installation, fetches its package's versions list from packagehub via `fetchWithCorrelation` (so the §4.3 trace ID flows), picks the latest non-yanked entry, and returns an outdated-set when newer than installed. Version comparison via a new `compareVersions()` helper — splits on `.`, numeric per segment (`1.10` > `1.9`, not lex), pre-release tags (`-rc.1`) order below the base, lexicographic on the suffix. New `GET /api/v1/workspaces/:wsId/updates` endpoint ([routes/installations.routes.ts](services/packages/src/routes/installations.routes.ts)) returns `{ workspaceId, updates, count }` and fire-and-forget-publishes one [PACKAGE_TOPICS.UPDATE_AVAILABLE](../packages/events/src/topics.ts) NATS event per outdated installation (payload: workspaceId / installationId / packageName / installedVersion / latestVersion). The packages svc optionally connects to NATS at boot — if NATS is unreachable, the service still boots and `/updates` skips publish. office-ui will surface these via the §3.5 notification center; the frontend NATS subscriber is a follow-up bundled with the §3.5 WS approvals push. 12 new tests in [tests/auto-update.test.ts](services/packages/tests/auto-update.test.ts) cover compareVersions edge cases, no-installations, installed-equals-latest, outdated-flagged, yanked-skip-fallback, no-downgrade, transient-failure-graceful, only-installed-status-considered.
- [x] **packagehub** — GitHub Sigstore OIDC attestation as an alternate signing path. New [services/packagehub/src/services/sigstore-verifier.ts](services/packagehub/src/services/sigstore-verifier.ts) wraps the upstream [`sigstore`](https://www.npmjs.com/package/sigstore) package and exposes a `SigstoreVerifier` interface with a single async `verify(bundleJson, payload, identity)` call. The verifier delegates to `sigstore.verify(bundle, payload, { certificateIssuer, certificateIdentityURI, ... })` which performs the full chain: Fulcio cert-chain validation against a TUF-pinned root + Rekor inclusion proof + signature check + identity match — all upstream-maintained code, no hand-rolled cryptography. The `pubkey_kind = 'sigstore-oidc'` branch in [verifyAgainstActiveKeys](services/packagehub/src/services/signing.ts) parses the stored "pubkey" as a JSON-encoded `{ issuer, subject }` identity (issuer URLs contain `://`, so a tagged separator like `:` is ambiguous — JSON sidesteps that without ad-hoc parsing); the stored "signature" carries the serialized cosign bundle directly. Schema migration [0005_sigstore_columns.sql](services/packagehub/migrations/0005_sigstore_columns.sql) widens `packages.publisher_pubkey`, `package_pubkeys.pubkey`, and `package_versions.signature` from `varchar(64)`/`varchar(256)` to unbounded `text` so the longer Sigstore identity strings (workflow URIs commonly exceed 64 chars) and JSON-format cosign bundles (kilobytes-scale) fit. `verifyAgainstActiveKeys` was made async and the four call sites in [pubkeys.routes.ts](services/packagehub/src/routes/pubkeys.routes.ts) + [versions.routes.ts](services/packagehub/src/routes/versions.routes.ts) gained `await` — the rotation flow now works under either kind: the publisher signs the rotation digest with cosign + their existing identity, the verifier matches against active keys uniformly. Three optional env vars (`SIGSTORE_TUF_MIRROR_URL`, `SIGSTORE_TUF_CACHE_PATH`, `SIGSTORE_TUF_FORCE_CACHE`) let air-gapped deployments pin a private TUF mirror without code changes. Test seam — `setSigstoreVerifier(mockVerifier)` swaps in a stub for unit tests; 14 new tests in [tests/sigstore-verifier.test.ts](services/packagehub/tests/sigstore-verifier.test.ts) cover identity-JSON parsing edge cases (missing fields, non-string types, malformed JSON, null literal) + dispatch logic (matching identity returns the JSON pubkey, mock-rejected bundle returns null, mixed Ed25519+Sigstore keys short-circuit on first match, unknown kinds skipped). 83 → 97 packagehub tests pass; typecheck + build clean. **Why not internet-bound integration tests**: they'd require a captured cosign bundle fixture + pre-warmed TUF cache + offline fall-back, which is more CI infra than this iteration warranted; the upstream `sigstore` package has its own test suite, and our wiring is covered by mock-based unit tests of the dispatch + identity paths. Today's Ed25519 path covers the "publisher proves they wrote the package" property; Sigstore now adds the "publisher's CI pipeline did, witnessed by a public log" property on top — closing the §6.3 signing trio (Ed25519 floor / pubkey rotation / OIDC attestation). End-to-end recipe in [docs/PACKAGES.md](docs/PACKAGES.md).
- [x] **packagehub** — Stripe webhook receiver at `POST /api/v1/webhooks/stripe` ([webhooks.routes.ts](services/packagehub/src/routes/webhooks.routes.ts)). Pure Node — no `stripe` SDK dep. Signature verification in [stripe-signature.ts](services/packagehub/src/services/stripe-signature.ts) computes HMAC-SHA256 of `${t}.${rawBody}` and timing-safe-compares to each `v1` value in `Stripe-Signature` (multiple v1 entries during secret rotation; any match passes). Default 5-minute replay window. Server.ts adds an `addContentTypeParser` wrapper that captures the raw JSON string into `request.rawBody` while still handing the parsed object to every other route. Webhook URL is on the `publicRoutes` list (Stripe doesn't speak JWT); the route enforces signature verification itself before touching the DB. `checkout.session.completed` mints an entitlement using the session id as `externalRef` — idempotent: a retried delivery returns the existing row with `idempotent: true`. Subscriptions propagate `subscription.current_period_end` into `entitlements.expires_at` so a lapsed sub flips off without manual revocation. Missing metadata, unknown package, and unsupported event types all return 200 (Stripe retries 4xx endlessly; operators diagnose via logs). 503 when `STRIPE_WEBHOOK_SECRET` is unset — explicit "not configured" beats silent acceptance. 14 new tests cover signature roundtrip, tampered body, secret mismatch, drift, missing/malformed inputs, idempotency, and the four event-shape branches. Lemonsqueezy is the same shape with a different signature algorithm — clean per-provider helper drop-in when needed.
- [x] **packages** — Persisted installation/version history (Drizzle table) so rollback survives service restarts. Two-table append-only schema in [services/packages/src/db/schema/](services/packages/src/db/schema/) — `installations` (id ULID, workspaceId, packageName, version, type, status, jsonb config, timestamps) + `version_history` (id ULID, installationId FK ON DELETE CASCADE, version, recordedAt). Append-only over a JSONB stack because the query pattern (`SELECT … ORDER BY recorded_at DESC`) is the rollback walk and the cascade cleans up history without app-level coordination. New `InstallationRepo` interface in [services/installation-repo.ts](services/packages/src/services/installation-repo.ts) with two implementations: `DrizzleInstallationRepo` for production and `InMemoryInstallationRepo` for tests — chosen over pg-mem because pg-mem's postgres-js adapter doesn't expose `pg.default`, so getting it past the require boundary needs a `pg-server` peer-dep dance and monkey-patches that don't pay for themselves on a 8-method surface. [PackageManager](services/packages/src/services/package-manager.ts) refactored to take an `InstallationRepo` instead of holding `Map`s — same external shape (`InstalledPackage`, all error codes, `injectInstallationForTest`), so every existing test in [tests/](services/packages/tests/) keeps passing; one new durability test in [rollback.test.ts](services/packages/tests/rollback.test.ts) creates a second `PackageManager` against the same repo to prove rollback walks across a simulated restart. Drizzle setup (config, migrations, [src/db/connection.ts](services/packages/src/db/connection.ts)) mirrors packagehub's. Dockerfile rewritten to Pattern A with a `migrator` stage between `builder` and `runner`. New `packages` Postgres database added to [init-db.sql](infra/compose/init-db.sql); `packages-migrate` job + `packages` service entry wired into [docker-compose.phase6.yaml](infra/compose/docker-compose.phase6.yaml) with `depends_on: packages-migrate (completed_successfully) + packagehub (healthy) + nats (healthy)` so the install path's entitlement check + UPDATE_AVAILABLE publishes have their dependencies up before the service starts. 56 tests pass (was 55 before the new durability test).
- [x] **packagehub** — Publisher pubkey rotation. New `package_pubkeys` table ([db/schema/package-pubkeys.ts](services/packagehub/src/db/schema/package-pubkeys.ts)) stores every key a publisher has ever associated with a package + an `active` / `revoked` status; ON DELETE CASCADE so dropping a package cleans rotation history. Migration [0004_package_pubkeys.sql](services/packagehub/migrations/0004_package_pubkeys.sql) backfills a row for every existing non-null `publisher_pubkey` so the verifier path keeps working without API churn. New `verifyAgainstActiveKeys()` in [signing.ts](services/packagehub/src/services/signing.ts) walks the active set and returns the matching key on success — both the version-publish path and the `/verify` endpoint now use it. Three new endpoints in [pubkeys.routes.ts](services/packagehub/src/routes/pubkeys.routes.ts): `GET /api/v1/packages/:name/pubkeys` (list), `POST` (add — proof-of-possession Ed25519 signature over `rotationDigest('add', name, newPubkey)` from any active key), `PATCH /:id` (revoke — same proof shape over `rotationDigest('revoke', …)`, refuses to revoke the last active key). The rotation digest binds the operation, package name, and target pubkey so a captured signature can't be replayed onto a different package or operation. Idempotent on add (re-registering the same pubkey returns the existing row); on package create with `publisherPubkey`, the original key is mirrored into `package_pubkeys` so the rotation table is the single source of truth from then on. Loss of every private key is intentionally unrecoverable on the API side — operators can manually `UPDATE` if a publisher loses everything; that's the deliberate floor. 9 new tests cover digest binding, list, add (success / proof mismatch / unsigned package), and revoke (success / last-key block / already-revoked). Total packagehub tests: 74 → 83.
- [x] **office-ui** — Marketplace UI: browse + detail + purchase flow. New `/office/marketplace` browse page ([page.tsx](apps/office-ui/src/app/office/marketplace/page.tsx)) lists packages with text search, type filter (`personality / skill / mcp_connector / design`), license-tier pill (`All / Free / Paid` — applied client-side until packagehub's `search.ts` grows a tier filter), verified-only checkbox, sort (`Relevance / Most popular / Recently published`). Cards show package name, type, author, description, up to 3 tags, install count, plus a license-tier badge (Free/Subscription/$X.XX) and inline icons for verified status + Ed25519 signing. Detail page at `/office/marketplace/[name]` ([page.tsx](apps/office-ui/src/app/office/marketplace/%5Bname%5D/page.tsx)) is a two-column layout — README + versions table on the left, sticky install/purchase CTA + meta panel + green-bordered Trust panel (only shown when `publisherPubkey` is set) on the right. Free packages POST to the *packages* service `/install` endpoint; paid/subscription tiers open a modal interstitial that links to `pkg.paymentLink` in a new tab. The 402 ENTITLEMENT_REQUIRED error path also triggers the interstitial so a stale-tier mismatch doesn't drop the user into a generic toast. New [marketplace-api.ts](apps/office-ui/src/lib/marketplace-api.ts) wrapper hits packagehub directly via `SERVICE_URLS.packagehub` (the default `api` client routes `/packages` to the install service); shares the same `urule-auth` token store so logged-in browse works without a separate sign-in. New "Marketplace" entry in the sidebar nav ([AppSidebar](apps/office-ui/src/components/layout/AppSidebar.tsx)). Stitch redesign hand-off captured in PR description; current implementation is functional MVP and the markup will swap one file per screen once designs come back.

See [docs/PACKAGES.md](docs/PACKAGES.md) for the publish-and-sign workflow with full bash recipes.

#### Package ecosystem follow-ups (open)
- [ ] **packagehub** — Sigstore real-bundle integration test (offline-runnable). Capture a real `cosign sign-blob --bundle` output for a known canonical-digest fixture, pre-warm the TUF cache via the `SIGSTORE_TUF_CACHE_PATH` env var, then run `sigstore.verify(bundle, payload, options)` end-to-end inside the test suite without network. Today's 14 sigstore-verifier tests use mocks (the upstream `sigstore` package has its own e2e tests, but our wiring against a real bundle is unverified). Plus negative-path tests: tampered bundle, wrong identity, expired cert.
- [ ] **mcp-gateway** — Tool invocation dispatch endpoint. New route `POST /api/v1/mcp/servers/:serverId/tools/:toolName/invoke` that (1) loads the bound MCP server's connection from the workspace binding, (2) validates `request.body` against the tool's input schema (already cached in the catalog), (3) proxies the call to the upstream MCP server, (4) returns the response. **Today the gateway has tool catalogs + workspace bindings + tool listing routes but no actual invocation path** — agents that need to call MCP tools have to go through langgraph-adapter's stack today. This is a real feature gap surfaced by the testing audit.
- [ ] **packagehub** — Download counter increment on install. `packages.downloads` is displayed in the marketplace card + sort but never actually bumped. Two paths: (a) packages-service install path fires `POST /api/v1/packages/:name/installs` against packagehub on success, OR (b) consume the existing `urule.packages.installed` NATS event (if the install service publishes it) on the packagehub side and increment from the consumer. Path (b) is cleaner — keeps install latency unchanged and survives transient packagehub outages.

### 6.4 Collaboration
- [x] **state** — Typing indicators for chat. New `TypingManager` ([services/state/src/services/typing-manager.ts](services/state/src/services/typing-manager.ts)) — short-lived (default 6s, matches Slack) "user is typing" flags scoped to `(userId, roomId)`. Subsequent pings refresh the expiry instead of duplicating; `listInRoom()` prunes expired entries as a side effect of read so the in-memory map doesn't grow unbounded. Three new routes: `POST /api/v1/rooms/:roomId/typing` (ping), `GET /api/v1/rooms/:roomId/typing` (poll), `DELETE /api/v1/rooms/:roomId/typing/:userId` (manual stop on submit). Pings scope per-room — same user can be typing in two rooms simultaneously. 6 new tests cover refresh, multi-user concurrent, multi-room scoping, expiry pruning.
- [x] **channel-router** — Email channel adapter ([adapters/email.adapter.ts](../channel-router/src/adapters/email.adapter.ts)). Normalizes inbound payloads from common transactional-mail provider webhooks (SendGrid/Mailgun/Postmark/SES `parse` endpoints): `from` field's "Display Name <addr>" gets split into `senderId` (lowercased address) and `senderName` (display name; falls back to local-part when no name). Body precedence: prefer `text`, fall back to a tag-stripped HTML approximation. Attachments mapped to canonical shape with `contentType`. Preserves `subject` / `messageId` / `inReplyTo` in `metadata` for thread correlation. Outbound `sendMessage` returns a Message-ID-shaped string (`<ulid@urule.local>`); actual SMTP relay integration is a follow-up.
- [x] **channel-router** — Discord channel adapter ([adapters/discord.adapter.ts](../channel-router/src/adapters/discord.adapter.ts)). Normalizes a Discord MESSAGE_CREATE gateway/webhook payload: `channel_id` → `channelId`, `author.global_name ?? author.username` → `senderName`, `content` → `text`. Attachments mapped via `content_type`. Metadata preserves `discordMessageId`, `guildId`, `replyTo` (from `message_reference.message_id`), and an `isBot` flag from `author.bot`. Both adapters registered in [server.ts](../channel-router/src/server.ts); `ChannelType` union extended to include `'discord'` (`'email'` was already there). 12 new tests across the two adapters cover RFC 2822 parsing, attachment mapping, metadata preservation, bot detection, reply-to extraction.
- [ ] **state** — Real-time collaborative editing (CRDT or OT). Substantial architectural work — Yjs-style document graph + room-scoped sync + persistence story. Defer until there's a UI surface that needs it (current widgets are read-mostly).
- [ ] **channel-router** — Microsoft Teams channel adapter. Similar shape to Slack/Discord but Teams' Activity payload is its own thing (BotFramework). Defer until there's actual Teams demand.

12 new tests in [channel-router/tests/email-discord-adapters.test.ts](../channel-router/tests/email-discord-adapters.test.ts) and 6 in [services/state/tests/typing.test.ts](services/state/tests/typing.test.ts).

#### Collaboration follow-ups (open)
- [ ] **channel-router** — Telegram adapter outbound message sending. Today's [telegram.adapter.ts](../channel-router/src/adapters/telegram.adapter.ts) only normalizes inbound webhooks; `sendMessage()` is a stub. Add a real POST to Telegram's Bot API (`https://api.telegram.org/bot<TOKEN>/sendMessage`) with config-driven token + chat-id resolution from the channel binding. Mirror the email adapter's "real wire-up is a follow-up" pattern.
- [ ] **channel-router** — Webhook adapter outbound forwarding + signature verification. The generic webhook adapter today copies inbound payloads but doesn't actually forward outbound messages anywhere — `sendMessage` returns `{ success: true, messageId: ulid() }` without HTTP. Add fetch-based forwarding to a configured target URL with optional `X-Hub-Signature-256` HMAC over the body using a per-binding shared secret.
- [ ] **approvals** — Temporal workflow integration. The `src/workflow/approval.workflow.ts` is a stub (~33 lines). Wire signal-based approve/deny/escalate decisions, expiration-timer enforcement (`expiresAt` is set today but no timer fires), and orchestrator resume signaling once the approval lands. Gates via env var `TEMPORAL_ADDRESS` so deploys without a Temporal cluster keep using today's in-memory state machine.
- [ ] **approvals** — Audit event emission to NATS subscribers. The state-machine builds an in-row `auditTrail` array on every transition (tested) but doesn't publish per-transition events to external consumers. Add `urule.approvals.transition.{requested,approved,denied,escalated,cancelled,changes-requested}` NATS topics so the office-ui notification center, audit-log warehouse, and future SIEM-export consumers can react in real time.

### 6.5 Office UI Features
- [x] **office-ui** — Data export for lists (CSV, JSON). [lib/exportData.ts](apps/office-ui/src/lib/exportData.ts) provides `toCsv()` (RFC 4180-quoted, union-of-keys columns), `toJson()`, `triggerDownload()` (SSR-safe), and `exportRows()` one-call serialize+download with timestamped filenames. [components/ui/ExportButton.tsx](apps/office-ui/src/components/ui/ExportButton.tsx) drops into any list view: outside-click and Escape close, disables when rows is empty, format dropdown (default CSV+JSON; restrict via `formats` prop). Test-IDs (`export-button`, `export-menu`, `export-format-csv|json`) for Playwright targeting.
- [x] **office-ui** — Cmd+K command palette. [components/layout/CommandPalette.tsx](apps/office-ui/src/components/layout/CommandPalette.tsx) mounted globally inside [office/layout.tsx](apps/office-ui/src/app/office/layout.tsx) so it works on every authenticated page. Subsequence-fuzzy filter, ↑↓/Enter/Esc keyboard navigation, click-outside dismiss, autofocus on open. Built-in commands: 7 navigation entries (dashboard/agents/chat/approvals/projects/workspaces/settings), 3 theme switchers (dark/light/system), 1 sign-out. State held in [store/useCommandPaletteStore.ts](apps/office-ui/src/store/useCommandPaletteStore.ts) so any component can `toggle()` programmatically. Cmd+K + Ctrl+K both bound — works on Mac and elsewhere.
- [x] **office-ui** — User preferences store. [store/useUserPrefsStore.ts](apps/office-ui/src/store/useUserPrefsStore.ts) (zustand+persist, namespace `urule-user-prefs`): UI density (`comfortable`/`compact`), landing page (which `/office/*` route to default to), per-route list filters (keyed by route slug, arbitrary serialisable shape), favorites (kind+id+optional label, dedup-on-add). `notificationSoundsEnabled` slot reserved for the deferred sounds work. `partialize` keeps actions out of localStorage.
- [x] **office-ui** — Form draft auto-save. New [hooks/useFormAutoSave.ts](apps/office-ui/src/hooks/useFormAutoSave.ts): pairs with react-hook-form's `watch()` (or any reactive value) and persists to localStorage on a debounce (default 800ms). On mount, restores the persisted draft via the `onRestore` callback (typically `reset(draft)`); returns `{ hasDraft, lastSavedAt, discardDraft, getDraft }` so callers can render a "Saved 3s ago / Discard" UI. Storage keyed under `urule-form-draft:<storageKey>` to avoid cross-form collision; default `isEmpty()` predicate skips persisting all-blank state so an empty mount doesn't clobber a real draft. SSR-safe (no-op on server). Heavy forms (agent creation wizard, package publish) opt in by adding `useFormAutoSave({ storageKey, value: watch(), onRestore: reset })` and calling `discardDraft()` on submit success — no other plumbing required.
- [x] **office-ui** — Undo/redo for form editing. New [useFormHistory](apps/office-ui/src/hooks/useFormHistory.ts) hook maintains past/present/future snapshot stacks scoped to a single form. Pairs with react-hook-form: `const value = watch(); useFormHistory({ value, onRestore: (snap) => reset(snap) })`. Snapshots push on a 600ms debounce so a paste-storm or pause-typing-burst coalesces to one history entry per natural breakpoint, not per character; an `applyingRef` flag blocks the restore-triggered value-change from re-pushing onto the stack (would otherwise create an undo→push→undo loop). Stack capped at 50 to bound the OOM surface from pathological agents. Drop-in [UndoRedoButtons](apps/office-ui/src/components/ui/UndoRedoButtons.tsx) component renders the icon-pair and wires global Cmd/Ctrl+Z + Cmd/Ctrl+Shift+Z; gated by an `isInEditableField()` check so the browser's native field-level undo stays primary inside `<input>` / `<textarea>` / `[contenteditable]` (override via `captureInFields`). Lives in memory only — pairs cleanly with [useFormAutoSave](apps/office-ui/src/hooks/useFormAutoSave.ts) which handles the orthogonal "survive a crash/reload" persistence concern. Form-page authors opt in: `<UndoRedoButtons history={history} />` in their toolbar; not retro-fitted onto every form, since the value/cost calculation is per-form (heavy editor pages benefit; a 3-field create-modal doesn't need it).
- [x] **office-ui** — Real-time notification sounds (configurable). New [hooks/useNotificationSounds.ts](apps/office-ui/src/hooks/useNotificationSounds.ts): subscribes to `useToastStore`, plays a short Web Audio sine-tone on each new toast, type-keyed (success: ascending two-note A5→C#6 chime; info: E5; warning: C5; error: F#4). Gated by `useUserPrefsStore.notificationSoundsEnabled` (off by default; toggle exposed via Cmd+K command palette). Tones are synthesized in code — no audio assets shipped. Lazy AudioContext creation respects browser autoplay policies (no-op until user interaction); silently no-ops when AudioContext is unavailable (headless tests, very old browsers). Conservative peak gain (0.06) avoids startling users. Wired globally in [office/layout.tsx](apps/office-ui/src/app/office/layout.tsx) so it works on every authenticated page.

Two new commands added to the Cmd+K palette: "Enable/Disable notification sounds" (`prefs-sounds-toggle`) and "Switch to compact/comfortable density" (`prefs-density-toggle`) — both flip values in `useUserPrefsStore` so they persist.

New Playwright spec [office-ui/e2e/command-palette.spec.ts](apps/office-ui/e2e/command-palette.spec.ts) covers: open via Cmd/Ctrl+K, Escape closes, filter narrows the list, Enter on a navigation command routes correctly, plus a CSV-roundtrip smoke for the export utility's quoting contract. 5 new specs across 2 journey files.

### 6.6 Operations
- [x] **infra** — Prometheus metrics collection. New `@urule/observability` workspace package exports a Fastify `metricsPlugin` (built on `prom-client` 15) that exposes `GET /metrics` returning the Prometheus exposition format. Default labels include `service`. `http_requests_total{method,route,status_code}` counter and `http_request_duration_seconds` histogram populated via an `onResponse` hook. Default Node.js process metrics (CPU, RSS, event-loop lag, GC) included via `collectDefaultMetrics`. Per-plugin-instance `Registry` keeps multiple Fastify apps in the same process from cross-contaminating. Wired into all 10 services with `/metrics` added to each service's `publicRoutes` (Prometheus can't auth). New `prometheus` Compose service in [docker-compose.infra.yaml](infra/compose/docker-compose.infra.yaml) with [prometheus.yml](infra/compose/prometheus.yml) scrape config — registry/packagehub/state/adapter/approvals targets. 8 new tests in `@urule/observability`; no service-level test changes (covered by integration smoke).
- [x] **infra** — Grafana dashboards for service monitoring. New `grafana` Compose service with anonymous-Viewer access for local dev (admin/urule for edits). Datasources (Prometheus + Jaeger) auto-provisioned via [grafana-datasources.yml](infra/compose/grafana-datasources.yml). Starter [Urule Services dashboard](infra/compose/grafana-dashboard-services.json) auto-provisioned with 6 panels: per-service RPS, p95 latency, error rate (5xx %), event-loop lag, RSS memory, CPU. Live-validated end-to-end: registry → /metrics → Prometheus → Grafana shows live data after a few requests.
- [x] **infra (§4.4 complement)** — `@urule/observability` also exports `initOtel(serviceName)` which initializes `@opentelemetry/sdk-node` with HTTP auto-instrumentations + OTLP gRPC trace exporter. Falls back to `OTEL_DISABLED=true` for tests. Now wired into all 10 services' `src/index.ts` entrypoints; otel-collector → jaeger pipeline live-validated end-to-end. See §4.4 above.
- [x] **infra** — Deployment guide ([docs/DEPLOYMENT.md](docs/DEPLOYMENT.md)). Covers the full phase6 stack (what's in it, ports, image sources), prerequisites, Pattern B host pre-build requirement, first-boot recipe + boot order, env-var configuration matrix (LOG_LEVEL / CORS_ORIGINS / KEYCLOAK_REALM_URL / AUTH_FAIL_CLOSED / OTEL_* / DATABASE_URL / NATS_URL / TEMPORAL_ADDRESS), reverse-proxy / TLS guidance with example Caddyfile, logging + log rotation, resource-limit starting points (derive from Prometheus + Grafana panels), upgrade flow, rollback caveats around forward-only migrations, scale-out sketch, troubleshooting matrix.
- [x] **infra** — Backup/recovery documentation ([docs/BACKUP-RECOVERY.md](docs/BACKUP-RECOVERY.md)). Tabulates what's stateful (postgres, nats, prometheus, grafana), what's not (all stateless services). Per-store: backup recipes (`pg_dumpall`, `nats stream backup`, prometheus tsdb snapshot, grafana volume tar + per-dashboard API export), recovery recipes (full restore, per-database, NATS stream restore), and security caveats (urule:urule password ends up in dumps; drizzle's `__drizzle_migrations` survives the round-trip). Recommends quarterly restore drills with a sample script.
- [x] **infra** — Helm charts for Kubernetes deployment. Umbrella chart at [infra/helm/urule/](infra/helm/urule/) deploys all 11 first-party services as a single release. Generic [deployments.yaml](infra/helm/urule/templates/deployments.yaml) + [services.yaml](infra/helm/urule/templates/services.yaml) loop over `.Values.services` so adding/removing a service is a values change, not a template change. Each entry parameterizes image basename, port, replica count, optional `databaseName` (drives the `DATABASE_URL` env via the [`urule.databaseUrl`](infra/helm/urule/templates/_helpers.tpl) helper), per-service env overrides, and resource requests/limits. Schema migrations run as Helm pre-install/pre-upgrade Job hooks ([migrate-jobs.yaml](infra/helm/urule/templates/migrate-jobs.yaml)) using the `<service>-migrator:<tag>` image convention — same multi-stage Dockerfile pattern the services already ship, just published as a separate tag. Optional [ingress.yaml](infra/helm/urule/templates/ingress.yaml) for office-ui (the only public-facing service); TLS gated by `ingress.tls.enabled` so it works on bare-cluster + gated-by-cert-manager setups. Stateful infra (postgres, nats, keycloak, openfga, opa) is deliberately NOT bundled — README documents the two reference patterns (managed services vs. Bitnami subcharts) and the env-var contract. `helm lint` passes; `helm template` renders 11 Deployments + 11 Services + 6 migration Jobs + 1 ServiceAccount + (optional) 1 Ingress.


---

## Summary

| Category | Priority | Items | Affects |
|----------|----------|-------|---------|
| Security | Critical | 25+ | All services |
| Testing | High | 20+ | All repos |
| UX & UI | High | 25+ | office-ui |
| Infrastructure | Medium | 20+ | All services, infra |
| Developer Experience | Medium | 20+ | All repos |
| Features | Low | 25+ | Various |

**Total: ~160 improvement items** (initial ~140 + 19 follow-ups identified 2026-05-08 from the recent shipped work + test-coverage audit; tracked under existing sections rather than a separate footer list)

---

*Last updated: 2026-05-08*
